# pdfcraftai.com — Deployment & Session Notes

_Last updated: 2026-04-22 (**Task #15 / Phase B item #1 CLOSED — credit_ledger expanded with fee/tax/FX/net columns.** Shipped in commit `1827ebf`. Migration `0012_credit_ledger_financials.sql` applied to prod MariaDB via SSH HEREDOC pipe BEFORE the code push (same discipline as migrations 0005-0011): 12 additive nullable columns on `credit_ledger` — `gross_charge_micros bigint`, `billing_currency char(3)`, `provider varchar(32)` with app-layer enum (`"paddle"|"razorpay"|"manual"|"refund_reversal"` — varchar preferred over mysqlEnum because adding a new provider in Task #20 Razorpay integration is a code change, not an ALTER TABLE), `processor_fee_micros bigint`, `tax_collected_micros bigint`, `tax_treatment varchar(16)` (`"mor"` Paddle MoR absorbs / `"forward"` Razorpay collects we remit / `"rcm"` foreign-buyer reverse charge / `"none"`), `tax_remittable_micros bigint`, `fx_rate_used decimal(18,8)` (precision exceeds IEEE-754 capacity past ~15 total digits — persisted + read as string via `String(fin.fxRateUsed)` guard, NEVER `parseFloat`/`Number()`), `fx_slippage_micros bigint`, `net_revenue_micros bigint` (USD micros canonical — the actual value the /admin/margin dashboard aggregates against `ai_usage.cost_micros` for true-cost margin), `card_fingerprint varchar(64)`, `data_source varchar(16)` (`"webhook"` authoritative / `"backfill_api"` provider REST history / `"estimate"` synthetic — /admin/margin will segment estimate rows separately to keep signal honest). Verified post-migration: 8 legacy cols + 12 new = 20 total, all new cols NULLable with NULL default, zero data loss, purely metadata ALTER on MariaDB so zero downtime. Drizzle schema extended (`db/schema/app.ts` adds `decimal` to mysql-core imports + 12 typed fields between `idempotencyKey` and `createdAt` with inline docstring comments enumerating the app-layer enum members for `provider`/`taxTreatment`/`dataSource`). New exported type `LedgerFinancials` in `lib/payments/ledger.ts` with strict TS literal unions matching the app-layer enum semantics; `GrantCreditsInput` extended with optional `financials?: LedgerFinancials` (zero breaking change — existing callers for promo grants / refund debits / internal bookkeeping continue to work unchanged); `tx.insert().values()` spreads every field with `?? null` fallback; `fxRateUsed` special-cased through `String(fin.fxRateUsed)` for decimal(18,8) precision preservation. New test harness `scripts/test-credit-ledger-financials.mjs` (73 assertions / 5 sections A-E: A migration shape — ALTER TABLE target + no ADD INDEX/KEY/UNIQUE + no REFERENCES + per-column `ADD COLUMN` regex + no NOT NULL; B Drizzle pins — decimal imported + creditLedger block extracted + per-column helper/length/precision regex; C ledger.ts wiring — LedgerFinancials exported + financials? present + all 11 literal-union members present + fxRateUsed `String()` guard + no parseFloat/Number + per-column `?? null` fallback; D cross-file invariant — every column name present in all three layers catches the "column added to migration but left out of write path" refactor trap; E additive safety on comment-stripped SQL — exactly 12 ADD COLUMN + no DROP/MODIFY/CHANGE in executable SQL + rollback playbook preserved in comments). Suite summary line `credit-ledger-financials: ${pass} passed, ${fail} failed` matches aggregator regex exactly. Aggregator `run-all-tests.mjs` SUITES gains `credit-ledger-financials` entry after `dev-hooks` with 15-line rationale comment (pure static-parse, no route imports, no live MySQL dependency, orthogonal to AI-cluster suites). Full aggregator at **1437/1437 passed**; `npx tsc --noEmit` exit 0. Pushed `c622e3d..1827ebf`. **Phase B payments-side chain begins.** Next: Task #16 (Paddle webhook handler populates these ledger columns from real `transaction.completed` / `adjustment.created` payloads — gated on Paddle KYC verification, currently in progress per CLAUDE.md §6, 3-7 business-day SLA). Previous: `f02c5b3` (2026-04-22, Task #14 / Phase A item #5 of 5 CLOSED — eval harness scaffold + per-op quality floor. Phase A code-only complete, 5/5. Migration 0011 applied to prod MariaDB via SSH: new `ai_eval_runs` table with 15 cols (`passed` 0|1, `score_rubric` JSON, `overall_score` in basis-points matching `ai_daily_margin.margin_bps`) and 3 query-pattern indexes (`op, created_at` for dashboard time-series, `run_batch_id` for grouping one CLI invocation's rows, `commit_sha, op` for CI drift detection). New module `lib/ai/eval/` with `types.ts` (11-member `RubricCheckKind` union, `OP_QUALITY_FLOOR` per-op trailing-7d pass-rate floors in bps), `rubric.ts` (11 pure deterministic primitives — zero I/O, zero randomness, zero Date.now so the Phase B floor alarm is real signal), `golden-set.ts` (8 v1 fixtures covering translate×3 / summarize / compare / rewrite / table / redact; deferred to Phase B: ocr / chat / generate / sign), `runner.ts` (`server-only` orchestrator: loops fixtures → builds minimal ChatInput via inline `PROMPT_BUILDERS` to isolate model behavior → `route(op)` + `provider.chat()` → score → persist; `scoreBps >= thresholdBps ? 1 : 0`; reads `COMMIT_SHA` from env for deploy traceability). New CLI `scripts/run-ai-evals.mjs` (`--dry-run` / `--ops=` / `--ids=` / `--stub=` / `--json` flags; dry-run dynamic-imports rubric.ts + smokes outputNonEmpty + noPreamble per fixture; live mode prints the forthcoming POST `/api/admin/ai-evals/run` body for Phase B hand-off). New test harness `scripts/test-ai-evals.mjs` at 153/153 assertions across 9 sections; aggregator wired `ai-evals` suite right after `ai-router`; full aggregator at 1364/1364 passed; `npx tsc --noEmit` exit 0. Pushed `383921c..f02c5b3`. **Phase A code-only chain complete:** Task #10 (prompt caching, `f2420a8`) → Task #11 (output caps + truncation observability, `9a9d455`) → Task #12 (per-user daily cost ceiling + kill switches, `a178f29`) → Task #13 (OpenAI Batch API adapter, `e256d2b`) → Task #14 (eval harness, `f02c5b3`). Phase B gated on Paddle KYC verification (in progress, 3–7 business-day SLA).)_

## Production environment

- **Host:** Hostinger (managed Next.js hosting, hPanel)
- **CDN / Proxy:** Cloudflare (proxy enabled — confirmed via `cf-ray`, `server: cloudflare`, `cf-cache-status: DYNAMIC`)
- **Domain:** https://pdfcraftai.com (apex + www redirect)
- **Current commit at last successful deploy:** `1827ebf` (2026-04-22, Task #15 / Phase B item #1 of N — credit_ledger financial columns. Migration `0012_credit_ledger_financials.sql` applied pre-push to prod MariaDB via SSH HEREDOC pipe (same discipline as 0005-0011): 12 additive nullable columns — `gross_charge_micros` / `billing_currency` / `provider` (varchar + app-layer enum so Razorpay addition in Task #20 is code-only not ALTER TABLE) / `processor_fee_micros` / `tax_collected_micros` / `tax_treatment` (mor/forward/rcm/none) / `tax_remittable_micros` / `fx_rate_used` decimal(18,8) / `fx_slippage_micros` / `net_revenue_micros` (USD micros canonical for /admin/margin roll-ups) / `card_fingerprint` / `data_source` (webhook/backfill_api/estimate). Verified via `SHOW COLUMNS FROM credit_ledger` — 8 legacy + 12 new = 20 cols, all new NULLable, metadata-only ALTER so zero downtime. Drizzle schema extended (added `decimal` to mysql-core import + 12 typed fields); new exported type `LedgerFinancials` with strict literal unions; `GrantCreditsInput` extended with optional `financials?: LedgerFinancials` (zero-breaking for existing callers). `fxRateUsed` persisted via `String(fin.fxRateUsed)` — decimal(18,8) precision exceeds IEEE-754 capacity past ~15 total digits, `parseFloat`/`Number()` would lossy-round. New test harness `scripts/test-credit-ledger-financials.mjs` (73 assertions / 5 sections: migration shape, Drizzle pins, ledger.ts wiring, cross-file invariant catching "column added to migration but left out of write path", additive safety on comment-stripped SQL). Aggregator wired `credit-ledger-financials` suite after `dev-hooks`; full aggregator at **1437/1437 passed**; `tsc --noEmit` exit 0. **Phase B payments-side chain begins here.** Previous: `f02c5b3` (2026-04-22, Task #14 / Phase A item #5 of 5 — eval harness scaffold + per-op quality floor. Migration 0011 applied pre-push to prod MariaDB via SSH (same discipline as 0005-0010): new `ai_eval_runs` table, 15 cols including `passed` 0|1 + `score_rubric` JSON + `overall_score` in basis-points, 3 query-pattern indexes on `(op, created_at)`, `(run_batch_id)`, `(commit_sha, op)`, InnoDB + utf8mb4_unicode_ci. MariaDB reports json as longtext with JSON_VALID check constraint — expected, not drift. New `lib/ai/eval/` module (types / rubric / golden-set / runner), new CLI `scripts/run-ai-evals.mjs` (`--dry-run` / `--ops=` / `--ids=` / `--stub=` / `--json`), new test harness `scripts/test-ai-evals.mjs` (153 assertions, 9 sections), aggregator wired `ai-evals` right after `ai-router`. Full aggregator at 1364/1364 passed; `tsc --noEmit` exit 0. **Phase A code-only (5/5) COMPLETE.** Phase B gated on Paddle KYC verification (in progress). Previous: `e256d2b` (2026-04-22, Task #13 / Phase A item #4 of 5 — OpenAI Batch API adapter for non-urgent ops. Routes `summarize` + `translate` through `/v1/batches` at 50% discount / 24h SLA. New migration `db/migrations/0010_batch_jobs.sql` creates the `batch_jobs` table (20 cols: `id` varchar(36) PK, `user_id` varchar(255) FK→users.id ON DELETE CASCADE, `op` varchar(32), `openai_batch_id` varchar(64), `status` varchar(32), `request_count` int, `idempotency_key` varchar(128), `spend_ledger_id` varchar(36), `credit_cost` int, `input_file_id` varchar(128) NULL, `output_file_id` varchar(36) NULL FK→files.id ON DELETE SET NULL, `error_file_id` varchar(128) NULL, `op_payload` json, `tokens_in` int NULL, `tokens_out` int NULL, `result_payload` json NULL, `error_message` varchar(512) NULL, `submitted_at` timestamp(3), `completed_at` timestamp(3) NULL, `created_at` timestamp(3)); UNIQUE `batch_jobs_user_idem_uq (user_id, idempotency_key)` gives replay idempotency; `(user_id, submitted_at)` + `(status, submitted_at)` indexes power the admin list + cron scan. Applied to prod MySQL via SSH HEREDOC BEFORE the code push; verified via `DESCRIBE batch_jobs` (20/20 columns) + `SHOW INDEX FROM batch_jobs` (PRIMARY + UNIQUE + 2 indexes + FK index). **Runtime surface — two new routes:** `POST /api/ai/batch/submit` (auth → `guardAiRoute(op, userId)` → parse `op ∈ {"summarize","translate"}` + `isBatchEligible` gate → 25MB PDF cap → op-specific param validation → `db.select().from(batch_jobs).where(and(userId, idempotencyKey))` replay check → `spendCredits({userId, operation: op, idempotencyKey: "ai:batch:{op}:{idem}"})` → extractPdfText (refund on fail) → `buildSummarizeBatchRequest` or `buildTranslateBatchPlan` per op → `submitBatch({requests, description, metadata: {op, user_id, batch_job_id}})` in the OpenAI Batch adapter (Files API JSONL upload + POST `/v1/batches` with `endpoint:"/v1/chat/completions"` + `completion_window:"24h"`) → persist `batch_jobs` row capturing `opPayload = {filename, depth | targetLang, pageCount, sourceSha256, chunkPlan, creditCost, spendLedgerId, spendIdempotencyKey, clientIdempotencyKey, customIdPrefix}` so finalize never needs the original PDF bytes) and `GET /api/ai/batch/[jobId]` (auth → WHERE `id = ? AND user_id = ?` for isolation → short-circuit on local terminal states `{finalized, failed, expired, cancelled}` → `pollBatch(openai_batch_id)` → on `{failed, expired, cancelled}` refund via original spend key + record error → on still-running return polling snapshot → on `completed` → `fetchBatchResults` → if `results.lines.length < request_count` treat as all-or-nothing failure (v1) + refund → op-dispatch to `finalizeSummarizeBatchResult` or `finalizeTranslateBatchResult` (wrapped in try/catch so moderation + reassembly errors refund + status=failed) → `recordAiUsage({batchMode: true, ...})` → `db.transaction` inserts `files` (toolId `ai-summarize` | `ai-translate`) + `ai_outputs` (kind `summary` | `translation`, `meta = {mode: "batch", openaiBatchId, batchJobId, ...}`, `idempotencyKey = clientIdempotencyKey`) + updates `batch_jobs → status="finalized"`). **Cost accounting:** `BATCH_DISCOUNT_MULTIPLIER = 0.5` in `computeCostMicros` routes the 50% OpenAI discount into `ai_usage.cost_micros` when `batchMode=true`, so the savings flow to **infra margin, not user price** (users pay the same credit price as realtime — no user-visible "cheaper if you wait 24h" option in v1). `ai_outputs.meta` stamps let the admin dashboard segment batch vs realtime volume. `npx tsc --noEmit` exit 0; pushed `2aad843..e256d2b main -> main`. **Deploy gotcha:** migration 0010 is NOT auto-applied by Hostinger — MUST be piped pre-push (same pattern as 0005-0009). Previous: `a178f29` (2026-04-22, Task #12 / Phase A item #3 of 5 — per-user daily cost ceiling + op/provider kill switches. Two-circuit spend defense: Circuit #1 env-flag kill switches `AI_KILL_<PROVIDER>` + `AI_KILL_<OP>` read per-call from `process.env` (operator flips in hPanel + redeploys → next request honors the flag, no code push needed; truthy vocabulary `"true"/"1"/"yes"/"on"` case-insensitive, anything else falsy so typos fail safe OFF), Circuit #2 per-user daily cost ceiling with `USER_DAILY_COST_MICROS_CAP=500000` default ($0.50/user/UTC-day) enforced via `SELECT COALESCE(SUM(cost_micros), 0) FROM ai_usage WHERE user_id = ? AND created_at ∈ [UTC-midnight, +24h)` using the existing `(user_id, created_at)` covering index, per-user override via new `user_rate_limits (user_id PK → users.id ON DELETE CASCADE, daily_cost_cap_micros bigint, notes varchar(256), created_at, updated_at)` table. Shared `guardAiRoute(op, userId): Promise<Response | null>` wired into all 10 op routes BEFORE `spendCredits` (503 `op_disabled` with `Retry-After: 60` on kill; 429 `daily_cost_ceiling_exceeded` with exact `Retry-After: <sec to UTC midnight>` on cap). Read-only admin page `/app/admin/kill-switches` surfaces the current env-flag matrix + env-default cap + override count. Migration 0009 applied pre-push to prod MySQL via SSH HEREDOC pipe — first attempt threw MySQL errno 150 because the FK initially referenced NextAuth's singular `user` table; prod uses Drizzle's pluralized `users` per `db/schema/auth.ts:21`, migration patched `user → users` before re-running clean, verified via `SHOW CREATE TABLE user_rate_limits`. `npx tsc --noEmit` exit 0. **Deploy gotcha:** migration 0009 NOT auto-applied — MUST be piped pre-push (same pattern as 0005/0006/0007/0008). Previous: `9a9d455` (2026-04-22, Task #11 / Phase A item #2 of 5 — output-cap centralization + truncation observability. 16-file bundle + one new migration: (1) new `db/migrations/0008_ai_usage_truncation_cols.sql` — additive `ALTER TABLE ai_usage` adding `stop_reason varchar(32) NULL AFTER success` + `response_truncated int NULL AFTER stop_reason` + covering index `ai_usage_truncated_created_idx (response_truncated, created_at)`. Applied to prod MySQL via SSH HEREDOC pipe BEFORE the code push, verified via `SHOW COLUMNS` + `SHOW INDEX`. Deliberate nullable-NOT-DEFAULT-0 so pre-migration rows stay "unknown" rather than falsely claiming "not truncated". (2) new `lib/ai/output-caps.ts` (275 lines, `server-only`) — single source of truth for output-token ceilings: `HARD_CEILING_TOKENS = 8192` (set at the lowest per-model output cap across the three providers), `OP_OUTPUT_CAP_TABLE` with per-op × per-variant caps for all 10 `AIOp`s, `capForOp(op, variant?)` resolver (unknown variant falls through to `"default"`, every return value already clamped to the hard ceiling as defense-in-depth), `clampToHardCeiling(n)` idempotent helper (NaN/Infinity/<1 collapse to 1, >8192 clamp down, else floor). Docstring cites the Net-Margin Task #11 audit evidence trail for every cap value. (3) `db/schema/app.ts` — adds nullable `stopReason: varchar("stop_reason", { length: 32 })` + `responseTruncated: int("response_truncated")` to the `aiUsage` table with `truncatedCreatedIdx: index("ai_usage_truncated_created_idx").on(t.responseTruncated, t.createdAt)`. (4) `lib/ai/usage.ts` — exports `TRUNCATED_STOP_REASONS: ReadonlySet<string> = new Set(["max_tokens"])` (all three adapters normalize to Anthropic's `"max_tokens"` vocabulary at the adapter boundary so we pattern-match on one string, not three). `isTruncatedStopReason(stopReason): number | null` is the 3-way classifier — `null` for null/undefined/empty/whitespace inputs (preserves "unknown" semantic), `1` if trimmed+lowercased reason is in the set, `0` otherwise. `RecordAiUsageInput` extended with optional `stopReason?: string | null` + `responseTruncated?: number | null`. Private `normalizeStopReason` trims + caps at 32 chars (matches column width); `normalizeTruncatedFlag` coerces to `{0, 1, null}` defensively. (5) `app/api/ai/chat/route.ts` — flipped hardcoded `maxTokens: 1024` to `capForOp("chat")`; success branch passes `stopReason: finalStopReason, responseTruncated: isTruncatedStopReason(finalStopReason)`; error branch passes `stopReason: finalStopReason ?? "error", responseTruncated: isTruncatedStopReason(finalStopReason)` (error rows stay distinguishable from naturally-terminated ones). (6) `app/api/ai/summarize/route.ts` — success branch passes both fields. (7-15) nine `lib/ai/*.ts` op modules — `ocr.ts`, `translate.ts`, `summarize.ts`, `compare.ts`, `generate.ts`, `sign.ts`, `rewrite.ts`, `table.ts`, `redact.ts` — migrated from local `MAX_TOKENS_*` constants to `capForOp(op, variant?)` / `clampToHardCeiling(n)` imports from the new module. `translate.ts`'s dynamic `maxTokensForChunk(chunk)` now routes its computed value through `clampToHardCeiling` for the hard-ceiling guarantee. `summarize.ts` type chain threads `StopReason` through the `SummarizeResult` interface + `runChat` return so the route handler receives the provider's terminal reason. (16) `scripts/test-ai-usage.mjs` — grew from 72 → 130 assertions with a new Section D (D1 migration 0008 DDL parity, D2 Drizzle schema parity, D3 `isTruncatedStopReason` contract, D4 chat-route both-branch wiring + `capForOp("chat")` replacement of hardcoded 1024, D5 output-caps centralization + 10/10 op-module migration invariant — loops over every `lib/ai/*.ts` op asserting each imports from output-caps AND invokes `capForOp` or `clampToHardCeiling`). **Verification:** `node scripts/test-ai-usage.mjs` → 130/130 passing. `npm test` now runs 12 suites / **1128 assertions** in ~1.3s all green (pdf-tools 17 + geo-router 148 + geo-waitlist 248 + ai-usage 130 + chat-context-cap 33 + ai-router 100 + health-ai 27 + ai-margin-rollup 96 + prompt-safety 129 + output-moderation 123 + dev-hooks 8). `tsc --noEmit` exit 0. **Deploy verification:** migration 0008 piped to prod MySQL 2026-04-22 pre-push; `SHOW COLUMNS FROM ai_usage LIKE 'stop_reason'` → `stop_reason varchar(32) YES NULL`; `SHOW COLUMNS FROM ai_usage LIKE '%truncat%'` → `response_truncated int(11) YES MUL NULL`; `SHOW INDEX FROM ai_usage WHERE Key_name = 'ai_usage_truncated_created_idx'` → 2-column BTREE on (response_truncated, created_at). **Deploy gotcha:** migration MUST be applied before code push (same pattern as 0005/0006/0007) — post-push, `recordAiUsage` INSERTs `stop_reason` + `response_truncated` columns that don't exist yet and the fire-and-forget error-swallow silently drops audit rows. **Back-compat:** pre-migration rows stay NULL in both new columns; the rollup's per-op truncation-rate query filters `WHERE response_truncated IS NOT NULL` so historical rows don't pollute the signal. **Ships zero user-visible change** — truncation-rate surfaces land at Task #18 (admin dashboard v2, Phase B). Previous: `f2420a8` (2026-04-22, Task #10 / Phase A item #1 of 5 — Anthropic prompt caching wired end-to-end. Ten-file bundle + one new migration: (1) `db/migrations/0007_ai_usage_cache_cols.sql` — additive `ALTER TABLE ai_usage` adding `cached_input_tokens int NULL AFTER output_tokens` + `cache_creation_input_tokens int NULL AFTER cached_input_tokens`. Applied to prod MySQL via SSH + `mysql -u u692382124_pdfcraftaiapp ...` BEFORE the code push, verified by `DESCRIBE ai_usage` showing both columns at the correct ordinal, nullable int, no default. Deliberate NULL-vs-0 distinction: NULL = "cache not applicable to this provider/call" (OpenAI/Gemini rows stay NULL forever), 0 = "Anthropic ran `cache_control` but nothing hit" — preserves the per-op cache-hit-rate dashboard's ability to filter on `IS NOT NULL` so Gemini can't pollute the denominator. No index — reads are `SUM(...) GROUP BY` aggregates already served by `ai_usage_provider_created_idx` + `ai_usage_created_idx`. (2) `db/schema/app.ts` — added nullable `cachedInputTokens` + `cacheCreationInputTokens` int columns to the Drizzle `aiUsage` table with an inline comment pinning the null-vs-zero semantic. (3) `lib/ai/types.ts` — extended `TokenUsage` with optional `cachedInputTokens?: number` + `cacheCreationInputTokens?: number` fields; all three fields (existing and new) are optional so non-Anthropic adapters need zero changes. (4) `lib/ai/adapters/anthropic.ts` — captures `cache_read_input_tokens` + `cache_creation_input_tokens` from the `message_start` usage event alongside `input_tokens` / `output_tokens`, emits them on the terminal `"done"` chunk's `usage` object only when non-zero (keeps the payload tight for the 95% of Anthropic calls where caching didn't fire, keeps OpenAI/Gemini adapters completely untouched). (5) `lib/ai/usage.ts` — `computeCostMicros()` extended from `(modelId, in, out)` to `(modelId, in, out, cachedIn?, cacheWriteIn?)`, pricing each input bucket at its own Anthropic-published rate: uncached 1× base, cache-read 0.1× base (CACHE_READ_MULTIPLIER), cache-write 1.25× base (CACHE_WRITE_MULTIPLIER); `RecordAiUsageInput` extended with optional cache fields; `recordAiUsage` now stores `cached_input_tokens` + `cache_creation_input_tokens` on the row and writes the cache-aware `cost_micros` at insert time. The nightly rollup at `lib/ai/margin-rollup.ts` (which only sums `cost_micros` per row) automatically gets correct daily costs — zero rollup code changes. (6-9) `lib/ai/{summarize,compare,generate,sign}.ts` — `cacheSystemPrompt: true` added to the `provider.chat()` calls on all four Anthropic-primary ops with the longest stable system prefixes (summarize's multi-paragraph style guide, compare's two-doc diff rules, generate's template library, sign's signature-specific extraction rules). Haiku 4.5's cacheable-prefix minimum is ~2048 tokens which may exceed the current ~250-token system prompts today, but Anthropic silently skips the cache rather than erroring when the prefix is too short — so the flag is safe to have on from day one and starts billing cache rates the moment any prompt grows past the threshold, with zero follow-up code change required. (10) `app/api/ai/summarize/route.ts` — forwards `cachedInputTokens` + `cacheCreationInputTokens` to `recordAiUsage` on success. (11) `app/api/ai/chat/route.ts` — captures both cache fields from the terminal `"done"` chunk into route-level locals, forwards them to `recordAiUsage` on both success and error paths. **Verification:** `tsc --noEmit` clean (RC=0, zero output lines). **Deploy gotcha:** migration 0007 is NOT auto-applied — must be piped to Hostinger MySQL BEFORE the code rolls, otherwise `recordAiUsage`'s INSERT throws "Unknown column 'cached_input_tokens'". Applied 2026-04-22 via `ssh ... 'mysql -u ... -e ...'` pipe against the file, verified by `DESCRIBE ai_usage` in the same SSH session. **Back-compat:** pre-migration rows stay NULL in both new columns; rollup's `SUM()` treats NULL as 0 for summation purposes — mathematically identical to pre-migration behaviour — so historical margin numbers don't shift. **Ships zero user-visible change** — admin dashboard will consume the new columns at Task #18 (Phase B, admin dashboard v2). Previous: `4139098` (2026-04-21, Tier 1–5 of the 3-provider cost-matrix follow-ups — 12-file bundle paired with the user motive "Quality results for user and Higher margin profits". **Tier 1 — router flip (M1/M2/M4):** `lib/ai/router.ts` gains three new first-class `AIOp`s (`rewrite`, `table`, `redact`), each with `openai` primary and `[anthropic, gemini]` ladder plus `AI_ROUTER_{REWRITE,TABLE,REDACT}` env overrides. `translate` primary flipped `gemini` → `openai` (M1) — gpt-4o-mini is ~4× cheaper than gemini 2.5 flash on short-form bilingual passes per the cost matrix §2. `lib/ai/rewrite.ts`, `lib/ai/table.ts`, `lib/ai/redact.ts` migrated from ad-hoc `chat` op routing to `route('<op>', { preferredId })` + `NoRoutableProviderError` → `NoAIProviderConfiguredError` catch (preserves the 503 `no_ai_provider_configured` surface shipped since Phase A1). **Tier 2 — cost-accuracy fix:** `lib/ai/usage.ts` adds a `MODEL_RATE_TABLE` keyed by exact `providerId + modelId` strings adapters report (claude-haiku-4-5 $1/$5 per Mtok, gpt-4o-mini $0.15/$0.60, gemini-2.5-flash $0.30/$2.50) plus a `computeCostMicros()` helper; `recordAiUsage()` now calls it at insert time so every new row lands with non-NULL `cost_micros`. Retired the stale $0.075/$0.30 Gemini 1.5 constant in `docs/ai/margin_scenarios.py`. `lib/ai/margin-rollup.ts:319 COALESCE(SUM(costMicros), 0)` now has real numbers to roll up. **Tier 3 — detect-point alarms:** `lib/ai/margin-rollup.ts` grew three alarm evaluators: `margin_drift` (today's median margin < 30-day trailing median by ≥ 5pp), `primary_share` (router-primary coverage < 70%), `dark_routing` (out-of-ladder provider IDs serving > 0 calls). `app/api/cron/ai-margin-rollup/route.ts` shouldPost path now includes `report.alarms.length > 0`; log payload surfaces `alarmCount` + `alarmKinds` so an ops tail sees which tripped. SQL shape matches §5 of `COST_MATRIX_3PROVIDER.md`. **Tier 4 — prompt quality + cap tightening:** fidelity clauses added to `lib/ai/rewrite.ts` (preserve proper nouns, product names, code identifiers, URLs, emails, numbers/dates/currency/units verbatim — explicit "12%" example), `lib/ai/summarize.ts` (preserve numeric precision exactly — "several hundred" ≠ "about 450"; page citations `[p. 3]`; no superlatives `critical`/`remarkable`/`crucial`/`vital` unless source uses that exact word; no preamble/postamble), `lib/ai/translate.ts` (explicit regex-style code-identifier rule: camelCase/PascalCase/SCREAMING_SNAKE/snake_case/kebab-case/backticked tokens pass through verbatim — fixes observed QA regression of gpt-4o-mini rendering `loadUser` → `cargarUsuario`), `lib/ai/table.ts` + `lib/ai/redact.ts` (JSON validation nudges: verify parses, escape `\\"` in text/reason, escape `\\n`, pad short rows to header cardinality). Cap tightening where QA showed headroom: rewrite `simplify`/`formal`/`casual` modes 2400 → 2000, table 3200 → 2800; no-preamble clauses save another 20-40 output tokens per call. **Tier 5 — ship gate:** `scripts/test-router.mjs` grew from 84 → 100 assertions (added rewrite/table/redact to `OPS`, replaced stale B3 "translate picks gemini" with "translate picks openai (M1 flip, 2026-04-21)", added B3 primary assertions for rewrite/table/redact, added SECTION E6/E7/E8 call-site refactor checks mirroring E1 (ocr.ts) / E2 (translate.ts) / E3 (summarize.ts)). `node scripts/test-router.mjs` → 100/100 passing. `npx tsc --noEmit` → exit 0, zero errors. Files changed: 12 (`app/api/cron/ai-margin-rollup/route.ts`, `docs/STATUS.md`, `docs/ai/margin_scenarios.py`, `lib/ai/margin-rollup.ts`, `lib/ai/redact.ts`, `lib/ai/rewrite.ts`, `lib/ai/router.ts`, `lib/ai/summarize.ts`, `lib/ai/table.ts`, `lib/ai/translate.ts`, `lib/ai/usage.ts`, `scripts/test-router.mjs`). **Deploy gotcha — none.** Pure code change, no migration, no new env var. Previous: `8f0ca81` (2026-04-21, Task #28 — output moderation on every AI response, PLAN_GAP_ANALYSIS SEV-0 closed. Output-side defense-in-depth companion to Task #26's input-side prompt-safety: every non-streaming AI response is now scanned for secret leaks, PII echo, and jailbreak-compliance chatter BEFORE egress. New `lib/ai/output-moderation.ts` (572 lines) exports `moderateOutput(text, { op })` + `assertOutputSafe(result, op)` + `OutputModerationBlockedError`, with `ModerationOp = PromptSafetyOp` type-equal alias so input/output op sets stay lock-stepped. **Pattern library — 18 subcategories across 3 categories:** 10 secrets (9 critical: anthropic_api_key, openai_api_key, github_pat_classic, github_pat_fine_grained, stripe_api_key, aws_access_key_id, slack_token, google_api_key, private_key_pem + 1 medium: jwt_token), 5 PII (us_ssn + credit_card_shape at high with Luhn check; email_address + us_phone_number + ipv4_address at low for observability), 3 jailbreak-echo (dan_mode_activated at high; role_break_confirmation + ignoring_prior_instructions at medium). Pure regex-only v1, zero config, ~3ms per 50-page OCR transcript. **Call-site integration — 9 non-streaming ops blocking + 1 streaming route advisory-only:** `summarize`/`translate`/`compare`/`generate`/`rewrite`/`table`/`redact`/`sign`/`ocr` all call moderate+assert on final markdown before return; on critical finding the route handler refunds credits + returns HTTP 502 `output_moderation_blocked` with `redactedPreview` only (never the raw match). Chat route (SSE) is advisory-only with console.warn — SSE is already on the wire by the time we'd have the full joined response; margin-rollup cron (Task #22) aggregates warn rates. **Special cases:** generate moderates BEFORE pdf-lib render (don't bake leaks into PDF bytes), ocr moderates ONCE on joined `## Page N` markdown not per-page (10× regex cost + misses page-boundary findings). **Test harness** `scripts/test-output-moderation.mjs` (436 lines / 123 assertions / 6 sections A–F: module contract, secret library, PII library, jailbreak library, moderate/assert behavior, call-site integration). `scripts/run-all-tests.mjs` SUITES gains `output-moderation` between `prompt-safety` and `dev-hooks` with 19-line rationale comment. **Test verdict:** `npm test` now runs **11 suites / 977 assertions in ~1.4s** all green (pdf-tools 17 + geo-router 148 + geo-waitlist 248 + ai-usage 72 + chat-context-cap 33 + ai-router 76 + health-ai 27 + ai-margin-rollup 96 + prompt-safety 129 + output-moderation 123 + dev-hooks 8). `npx tsc --noEmit` exit 0. **Deploy verification:** `curl https://pdfcraftai.com/api/health` post-deploy returned `{ ok: true, commit: "8f0ca81eecdc", uptimeSec: 402, db: { ok: true, latencyMs: 7 }, ai: {…} }` — commit flip `88246b5` → `8f0ca81` verified at 14:20 UTC 2026-04-21. **Deploy gotcha — none.** Pure code change, no migration, no new env var. **Scope:** Task #28 closes `completed` — moderation has no runtime gate beyond "does `moderateOutput` run on every non-streaming op response," 9-call-site integration tests + live deploy verify end-to-end. Out of scope non-goals: (i) streaming-chat blocking (advisory-only by design — SSE on wire), (ii) model-based semantic moderation (2× latency+cost, v1 is pure-regex deterministic), (iii) malware scan on uploaded PDFs (Task #27, blocked on founder provider decision), (iv) DLP-style content classification (out of scope for regex v1). Previous: `f90846f` (2026-04-21, Task #26 — prompt-injection defense across every PDF→AI flow, PLAN_GAP_ANALYSIS SEV-0 closed. Defense-in-depth bundle across 13 files / +1169 / -15: new `lib/ai/prompt-safety.ts` (486 lines) is the single source of truth — exports `PromptSafetyOp = AIOp | "rewrite" | "table" | "redact"` superset type (keeps safety decoupled from Task #21 router since rewrite/table/redact still use legacy `selectProvider`), XML sentinel constants (`<untrusted_input source="…">…</untrusted_input>`), `buildSafetyPreamble(op)` keyed on `OP_SAFETY_TAIL` covering all 10 ops (ocr/translate/chat/summarize/compare/generate/sign/rewrite/table/redact), `wrapUntrustedInput(text, { sourceLabel })` applying source-label sanitization (allow-list `[A-Za-z0-9_\-:. ]`, 64-char cap) + U+200B zero-width-space break-out escape on literal `</untrusted_input>` or legacy `===` markers so attackers can't close the sentinel early, `detectJailbreakPatterns(text)` advisory-only detector with 13-pattern library (6 high-severity: direct role-swap / system-prompt-leak / instruction-override / tool-injection-exfil / credential-exfil / markdown-link-exfil; 5 medium: persona-swap / delimiter-confusion / locale-switch / code-block-leak / repeat-instruction; 2 low: flattery / urgency) returning `{ severity, patternId, match }[]` attached to `ai_usage.meta` for audit without blocking the user's call. 10 call-sites with distinct source labels per op semantic: summarize→`pdf_text`, translate→`source_text`, compare→`document_a_original`+`document_b_revised` (two wraps so the model can name each doc distinctly), generate→`user_prompt`, rewrite→`pdf_text`, table→`pdf_text`, redact→`pdf_text`, sign→`pdf_text`, chat→`pdf_attachment` plus every user chat message wrapped individually (multi-turn injection can't cascade), ocr→preamble-only (no wrap because OCR content is a base64 `DocumentBlock` not text; inline rationale comment explains OCR is uniquely vulnerable because the PDF image can visually render instructions the model will OCR and then be tempted to follow). New 592-line / 129-assertion test harness `scripts/test-prompt-safety.mjs` with 6 sections: A module contract (exports, type shape, sentinel constants, op coverage), B wrap/escape semantics (source label sanitization matrix, U+200B break-out on both marker types, nested wrap idempotence), C per-op safety preamble coverage (every `PromptSafetyOp` has a distinct tail), D jailbreak pattern library (all 13 patterns with positive+negative cases, severity ordering, regex disjointness), E call-site integration (every `lib/ai/*.ts` op module imports from prompt-safety AND calls wrap/preamble; chat wraps both PDF text + every user message; ocr uses preamble-only), F AIOp↔PromptSafetyOp alignment (superset invariant holds). `scripts/run-all-tests.mjs` SUITES gains `prompt-safety` entry between `ai-margin-rollup` and `dev-hooks` with 12-line placement rationale. **Test verdict**: `npm test` now runs **10 suites / 854 assertions in ~1.3s** all green (pdf-tools 17 + geo-router 148 + geo-waitlist 248 + ai-usage 72 + chat-context-cap 33 + ai-router 76 + health-ai 27 + ai-margin-rollup 96 + prompt-safety 129 + dev-hooks 8). `npx tsc --noEmit` exit 0. **Deploy verification**: post-push auto-deploy completed in ~4 min, 503 transition window cleared without manual `pkill next-server` (single next-server worker cycle per CLAUDE.md §5 — no stuck-worker cascade). `curl https://pdfcraftai.com/api/health` returned `{ ok: true, commit: "f90846f0b7ff", uptimeSec: 478 }`. **Deploy gotcha — none.** Pure code change, no migration, no new env var required. **Scope: Task #26 closes `completed`** on this deploy — prompt-safety has no runtime gate beyond "does the wrapper execute on every user-text path," which the 10-call-site integration tests + live deploy verify end-to-end. Out of scope (documented non-goals): LLM-output moderation (Task #28), malware scan on uploaded PDFs (Task #27), dynamic jailbreak-pattern blocking (detection is advisory-only by design; structural wrap is the actual defense). Previous: `0ba0665` (2026-04-21, Task #22 code-side — daily margin rollup cron + 7-day green streak metric / MASTER_PLAN §7 gate #7 / Phase A4. Seven-file bundle: (1) new `db/migrations/0006_ai_daily_margin.sql` (17 columns: id varchar(36) PK, date DATE, provider_id varchar(32), model varchar(128), operation varchar(32), 3 int counters call/success/error_count, 6 bigint sums input_tokens/output_tokens/latency_ms/credits_spent/cost_micros/revenue_micros, margin_bps/floor_bps int, is_green int, created_at timestamp(3) default current_timestamp(3)), composite UNIQUE `ai_daily_margin_slice_idx (date, provider_id, model, operation)` + 3 regular indexes (`date_idx`, `date_green_idx`, `provider_date_idx`). Docstring cites gate #7 + Phase A4 explicitly and explains the revenue-proxy methodology (30,000 µUSD/credit midpoint of Creator $0.036 + Pro $0.027 per-credit pricing — `credit_ledger` records debits in credits not USD so margin math needs a reference price). (2) `db/schema/app.ts` edit — `date` added to drizzle-orm/mysql-core imports, `aiDailyMargin` mysqlTable export appended (~95 lines): `date({ mode: "string" })` for clean YYYY-MM-DD handling, `bigint({ mode: "number" })` for the 6 sum columns, uniqueIndex + 3 regular index declarations matching the migration byte-for-byte. (3) new `lib/ai/margin-rollup.ts` (~430 lines) — Phase A4 analytical core. Exports `REFERENCE_USD_MICROS_PER_CREDIT = 30_000` revenue proxy, `OP_MARGIN_FLOOR_BPS` ladder (chat_turn/generate 60%, summarize/rewrite/table/sign 65%, translate/ocr/compare/redact 70% from MARGIN_VERIFICATION.md §12.3 S1 worst-case column), pure math (`computeMarginBps(revenueMicros, costMicros)` returns MIN on zero revenue + clamps to ±10000, `revenueMicrosFromCredits`, `floorForOp`), UTC helpers (`utcDateString` zero-padded YYYY-MM-DD, `utcDayStart`), async surface (`runDailyRollup({ targetDate? })` grouped-by query using Drizzle `sql\`COUNT(*)\`` + `COALESCE(SUM(CASE WHEN success = 1 THEN ...))` + idempotent upsert via `onDuplicateKeyUpdate({ set: { ...col: sql\`VALUES(col)\` } })`; `computeGreenStreak({ throughDate?, maxDays? = 90 })` walks backward day-by-day with explicit absent-day=not-green semantics preventing silent streak extension through outage windows; `postMarginAlertToSlack(report)` non-throwing so broken webhook never cascades to 500; `listConfiguredModelsForDate`). `"server-only"` boundary. Types `SliceReport` + `DailyRollupReport`. (4) new `app/api/cron/ai-margin-rollup/route.ts` — mirrors reconcile-payments auth pattern: `x-cron-secret` header, 500 on missing CRON_SECRET env, 401 on mismatch, POST + GET both call `runCron(req)`, `?date=YYYY-MM-DD` backfill accepted, Slack gate formula `redCount > 0 || (allGreen && greenStreakDays >= 7)` (posts on breach OR on gate-close celebration), structured `[ai-margin-rollup] report` log line for durable ops trail, `dynamic = "force-dynamic"` + `runtime = "nodejs"` + `maxDuration = 300`, `margin_rollup_failed` 500 on exception. (5) new `scripts/test-ai-margin-rollup.mjs` (~600 lines / 96 assertions across 7 sections): A migration SQL columns + indexes + types + defaults, B Drizzle schema import/export/type alignment with migration, C library public surface (constants + all 8 functions + server-only + onDuplicateKeyUpdate + AI_SPEND_ALERT_SLACK_URL env read), D pure-math behavior (REFERENCE=30000, 10-op floor map, compute returns MIN on zero revenue, clamp range, utcDateString zero-pad, streak-walks-backward), E cron route (POST+GET, runtime/dynamic/maxDuration, auth pattern, 500-on-missing/401-on-mismatch, ?date= backfill, Slack gate formula, console.log, 500-on-throw), F aggregator wiring pins, G spec-docstring cross-refs (gate #7 + phase A4 + proxy rationale). Canonical `AI-margin-rollup tests: 96 passed, 0 failed` summary line. (6) `scripts/run-all-tests.mjs` SUITES array gains new entry between `health-ai` and `dev-hooks` with a 10-line rationale comment explaining placement (same ai_usage schema dependency as Phase A1 — column rename breaks both, this harness pins the aggregate-side consumer). (7) `docs/STATUS.md` — section header bumped to `Phase A1 + A2 + A4 — gates #3 / #5 / #6 / #7`; new Task #22 entry at top of AI observability section with deploy gotcha + ops follow-up + gate-close criteria. **Test verdict**: `npm test` now runs 9 suites / 725 assertions in ~1.2s, all green (pdf-tools 17 + geo-router 148 + geo-waitlist 248 + ai-usage 72 + chat-context-cap 33 + ai-router 76 + health-ai 27 + ai-margin-rollup 96 + dev-hooks 8). `npx tsc --noEmit` exit 0. **Deploy gotcha (mirrors Task #19 pattern):** `0006_ai_daily_margin.sql` is NOT auto-applied on deploy — must be piped to Hostinger MySQL manually BEFORE first cron invocation (same SSH + `grep -v '^--> statement-breakpoint' | mysql -h ...` flow as `0005_ai_usage.sql` on 2026-04-21 ~11:00 UTC), otherwise the rollup's INSERT-on-duplicate throws and the cron returns `margin_rollup_failed` 500. **Ops follow-up:** user adds `15 0 * * * curl -H "x-cron-secret: $CRON_SECRET" https://pdfcraftai.com/api/cron/ai-margin-rollup` to hPanel → Advanced → Cron Jobs (00:15 UTC = previous day + 15min tail-latency headroom) once the migration has landed. **Gate #7 close criteria:** Task #22 stays `in_progress` until (a) migration applied AND (b) first cron writes rows AND (c) 7 consecutive all-green days accumulate — the gate-close celebration Slack message is the canonical signal. Previous: `f3c79e0` (2026-04-21, Task #18 code-side — `/api/health` now surfaces `ai: { configured, providers, defaults }` metadata block. Three-file code bundle: `app/api/health/route.ts` gains four new imports (`listConfiguredProviderIds` from `@/lib/ai/registry`, `currentPolicySnapshot` + `AIOp` type from `@/lib/ai/router`, `AIProviderId` type from `@/lib/ai/types`), a new `probeAi()` helper wrapping both introspection calls in try/catch so a registry-layer throw degrades to `{ configured: false, providers: [], defaults: {} }` + a `console.error` rather than cascading to 503 (DB liveness stays the only signal that flips `ok` — AI state is deployment state, not health state), and a one-line `ai` field wired into the JSON response between `db` and `ts`. Docstring rewritten to pin the new shape and the "AI never flips `ok`" invariant. New test harness `scripts/test-health-ai.mjs` (260 lines / 27 assertions / 5 sections: A imports, B `probeAi()` shape + try/catch degrade, C GET body wiring + 200/503 posture preserved, D router/registry export surface intact, E sibling posture with `/api/payments/probe` — no SDK imports, no `listConfiguredProviders` heavy variant, no `route()`/`getProvider()` calls, no direct `process.env.*API_KEY` read in the health route). `scripts/run-all-tests.mjs` SUITES gains `health-ai` entry between `ai-router` and `dev-hooks` with a rationale comment explaining the consumer-side pinning of the Task #21 router surface. `npm test` now runs **8 suites / 629 assertions in ~1.4s**, all green (pdf-tools 17 + geo-router 148 + geo-waitlist 248 + ai-usage 72 + chat-context-cap 33 + ai-router 76 + health-ai 27 + dev-hooks 8). `npx tsc --noEmit` exit 0. **Deploy gotcha — none.** Pure code change, no migration, no new env var required to deploy. **Task #18 stays in_progress (not completed):** this closes the CODE half of the `/api/health ai.configured probe` clause; the ENV-VAR half (user sets `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` in hPanel → App → Environment Variables, clicks Save & Redeploy) remains user-blocked — same pattern as Task #21's `GEMINI_API_KEY` deferred handoff. Once keys land, the probe flips `configured: false, providers: []` → `configured: true, providers: [<ids>]` **automatically** via the registry's env-driven rollout model — no code change, no redeploy beyond env. Gate #1 closes when a post-env-save `curl https://pdfcraftai.com/api/health | jq .ai` returns a non-empty `providers` array. Previous: `ed6e244` (2026-04-21, Phase A2 gate #6 — Gemini adapter + per-op routing ladder — Task #21. Three new files: `lib/ai/adapters/gemini.ts` (`GeminiProvider` class matching the `AIProvider` interface — gemini-2.5-flash default with `GEMINI_MODEL` override, PDF bytes as `inlineData` Part, usage from `usageMetadata`), `lib/ai/router.ts` (exports `AIOp`/`ROUTING_POLICY`/`OP_REQUIRED_CAPABILITY`/`OP_ENV_VAR`/`route()`/`NoRoutableProviderError` with caller-preferred > env-override > compiled-in precedence; ocr/translate → Gemini-primary, chat → OpenAI-primary, summarize/compare/generate/sign → Anthropic-primary with typed fallback chains), `scripts/test-router.mjs` (76 assertions / 6 sections). Seven refactored files: `lib/ai/types.ts`/`registry.ts`/`ocr.ts`/`translate.ts`/`summarize.ts`/`compare.ts`/`app/api/ai/chat/route.ts` — every `selectProvider()` → `route(op, { preferredId })` + typed `NoRoutableProviderError` catch mapped to each subsystem's existing error class. Chat route provider-swap preserved all Phase A1 `recordAiUsage`/`providerStartedAt` + Phase A2 `estimatePromptTokens`/20k-cap/413-refund logic byte-for-byte (verified green by re-running `test-ai-usage.mjs` + `test-chat-context-cap.mjs` post-refactor). `scripts/run-all-tests.mjs` SUITES gains `ai-router`; `npm test` now runs 7 suites / 602 assertions / ~1.1s, all green. `npx tsc --noEmit` exit 0. `@google/generative-ai ^0.24.1` added with clean +10-line lockfile delta. **No deploy gotcha** — pure code change, no migration, no env-var addition required to deploy (gate #6 runtime validation defers until `GEMINI_API_KEY` lands in Hostinger — same handoff shape as Task #18's `ANTHROPIC_API_KEY`). Until then `route("ocr", …)` falls through to the Anthropic fallback (if configured) or throws `NoRoutableProviderError` → 503 `no_ai_provider_configured` — the ladder degrades gracefully, doesn't silently misroute. Previous: `e882eed` (Phase A2 token-level input-context cap on `/api/ai/chat` — Task #20. Replaces old 16k-char byte-level `message_too_long` guard with a proper 20k-token ceiling per MASTER_PLAN §4 D4 + §7 gate #5. Three-file bundle: new `lib/ai/tokens.ts` (char-based heuristic, 3.5 chars/token Latin + 1:1 CJK + 4-token role-framing), restructured `app/api/ai/chat/route.ts` (cap check runs after PDF extract, before user-message INSERT; refund-before-413 on overflow), new `scripts/test-chat-context-cap.mjs` (33 assertions across 5 sections: static / behavioral / route-wiring / drift / spec-pin). Also wired ai-usage + chat-context-cap suites into the aggregator and fixed the Task #19 retrospective gap. `npm test` now runs 6 suites / 526 assertions / ~1.0s, all green. **No deploy gotcha this time** — pure code change, no migration. Previous: `037f6ea` Phase A1 `ai_usage` per-call audit table — Task #19. **Deploy gotcha for that one: `db/migrations/0005_ai_usage.sql` was applied manually to Hostinger MySQL 2026-04-21 ~11:00 UTC via SSH — verified via `SHOW CREATE TABLE ai_usage` showing all 15 columns + 5 indexes + FK to users.id ON DELETE CASCADE.** See STATUS.md § "AI observability (Phase A1/A2)". Before that: `5f70cd7` CF-IPCountry auto-preselect on `/launch-notify` Task #3 sub-item 4d; code shipped in `00615d2`.)

## Hostinger environment variables (production)

Set in hPanel → App → Environment Variables:

| Key | Value |
|---|---|
| `MYSQL_URL` | (pre-existing — MySQL connection string) |
| `NEXTAUTH_SECRET` | (pre-existing) |
| `NEXTAUTH_URL` | `https://pdfcraftai.com` |
| `NEXT_PUBLIC_SITE_URL` | `https://pdfcraftai.com` |
| `GOOGLE_CLIENT_ID` | `912612566698-n1857n8qa60n2sb55qag7sn2fi9bgias.apps.googleusercontent.com` |
| `GOOGLE_CLIENT_SECRET` | (set; do not echo) |
| `ADMIN_EMAILS` | *(optional)* comma-separated lowercase email allowlist for `/api/admin/*` endpoints. If unset or empty the founder (`rajasekarjavaee@gmail.com`) is the sole admin — setting `ADMIN_EMAILS` lets ops add more without a code change. Whitespace is trimmed, casing is normalised, entries without `@` are discarded. |

After editing env vars, click **Save and redeploy**.

## Google OAuth

- **Google Cloud project:** `pdfcraftai`
- **Consent screen:** Published + Branding verified (2026-04-19)
- **OAuth client type:** Web application
- **Authorized JavaScript origins:**
  - `https://pdfcraftai.com`
  - `https://www.pdfcraftai.com`
- **Authorized redirect URIs:**
  - `https://pdfcraftai.com/api/auth/callback/google`
  - `https://www.pdfcraftai.com/api/auth/callback/google`
- **App logo uploaded:** `public/brand/pdfcraftai-mark-120.png` (120×120 chromatic monogram)
- **Branding URLs filled:** App home, Privacy, Terms — all pointing at `https://pdfcraftai.com/...`
- **Support email:** `rajasekarjavaee@gmail.com` (swap to `support@pdfcraftai.com` once that mailbox is confirmed deliverable)

## Known operational issue — 503 after deployment

**Symptom:** Occasionally after clicking *Save and redeploy* in Hostinger, the site returns HTTP 503.

**Fix:** In Hostinger hPanel:
1. Go to **Resource usage** (left nav / app dashboard)
2. Find the running Node process(es)
3. Click **Stop running process**
4. The app auto-restarts fresh and the 503 clears

## Integration status (verified 2026-04-20)

| Integration | Status | Evidence |
|---|---|---|
| Cloudflare proxy | OK | `cf-ray`, `server: cloudflare` on every response |
| `robots.txt` | OK | Advertises `Sitemap: https://pdfcraftai.com/sitemap.xml` |
| Sitemap (`/sitemap.xml`) | OK | 39 URLs, application/xml, resubmitted to GSC + Bing 2026-04-19 |
| Google OAuth (plumbing) | OK | `/api/auth/providers` shows Google wired to correct callback |
| Google OAuth (sign-in smoke test) | Pending | Needs human click at `/login` |
| Microsoft Clarity | OK (live) | Tag `wcsbv536zv` present in rendered HTML, commit `36034eb` |
| Google Analytics (GA4) | OK (live) | Tag `G-2Y8PS0S93F` present in rendered HTML, commit `36034eb` |

## `app/layout.tsx` current state

Contains:
1. Theme-flash-prevention inline script (pre-existing)
2. GA4 snippet via `next/script` (id `ga4-init`, `afterInteractive`)
3. Microsoft Clarity snippet via `next/script` (id `ms-clarity-init`, `afterInteractive`)

IDs are defined as constants at the top of the file: `GA_MEASUREMENT_ID`, `CLARITY_PROJECT_ID`.

## Useful commands

```bash
# Check live headers (from sandbox)
curl -sI https://pdfcraftai.com | head -20

# Verify Clarity + GA4 present in live HTML
curl -s https://pdfcraftai.com | grep -oE '(gtag/js\?id=G-[A-Z0-9]+|clarity\.ms|ga4-init|ms-clarity-init)' | sort -u

# Check sitemap URL count
curl -s https://pdfcraftai.com/sitemap.xml | grep -c '<loc>'

# Auth plumbing
curl -s https://pdfcraftai.com/api/auth/providers
```
