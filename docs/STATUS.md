# pdfcraftai.com — Live Status & Punch List

_Single source of truth for what's done, what's pending, and who owns each item._
_Future Claude sessions: read this AFTER `CLAUDE.md` and BEFORE starting new work._

**Last updated:** 2026-04-21 (**Paddle MoR signup COMPLETE — Seller ID `320957`.** Vendor account created under `rajasekarjavaee@gmail.com` on 2026-04-21, all 7 onboarding screens cleared (business details, account type = Proprietorship, product + AUP descriptions, 5-URL website verification, business KYC with age-of-business 2026-04-16 backed by Hostinger invoice `H_41528083`, compliance screening 4/4). Verification status: **In progress** (3–7 business day SLA) — sandbox available immediately, production gated behind Paddle KYC review. **Adapter scaffolded** at `lib/payments/adapters/paddle.ts` (681 lines): implements the `PaymentProvider` contract for both one-time + subscription checkout via `POST /transactions` with `custom_data.internalPaymentId` portability anchor; webhook verification with `paddle-signature: ts=…;h1=…` HMAC-SHA256 + 10-min replay window + 2-min future-drift tolerance; event normalization for 10 Paddle event types mapping to our 5-kind `NormalizedPaymentEvent` union; refund via `POST /adjustments` (partialRefunds off pending sandbox validation); reconciliation async generator with cursor pagination via `meta.pagination.next`. Sandbox-vs-live host selection via `PADDLE_ENV`. All live-API call sites tagged `TODO(paddle-sandbox)` for hands-on validation once the keys are in. Wired into `lib/payments/registry.ts` as the fourth adapter row with a 5-env-var gate (`PADDLE_API_KEY` + `PADDLE_CLIENT_TOKEN` + `PADDLE_WEBHOOK_SECRET` + `PADDLE_SELLER_ID` + `PADDLE_ENV`). Webhook route mirrors Razorpay at `app/api/webhooks/paddle/route.ts`. Secrets template gains a `--- Paddle MoR ---` section with sandbox-vs-live key-prefix hints. `CLAUDE.md §3` paper-trails Seller ID 320957. `npx tsc --noEmit` exit 0. **Unblocks Task #1** (moved from pending → in_progress): next step is user-side sandbox key generation (`Developer Tools → Authentication`) and paste-in of `PADDLE_API_KEY` + `PADDLE_CLIENT_TOKEN` + `PADDLE_WEBHOOK_SECRET`, at which point end-to-end sandbox checkout becomes runnable. Earlier today: **CA_CONSULT_PREREAD.md shipped (`37dbe74`)** — 570-line / ~78 KB deep-research pre-read memo covering all 15 CA-confirmation items (Q1-Q7 from `TAX_MODEL.md §7` + GST-A through GST-H from `GST_SETUP.md §14`). For each question: indicative answer → legal basis with section/notification/circular/case-law citations → web-research findings → confidence level (3 Medium / 12 High, Medium items prioritised for the call) → primary-source hyperlinks → 3 specific questions to confirm with the CA. Research surfaces one notable departure from the canonical docs: Q6 (QRMP quarterly-return option) appears usable sub-₹5Cr turnover — `TAX_MODEL.md` + `GST_SETUP.md` currently default to conservative monthly filing; flagged in the memo for CA sign-off before the canonical stance flips. Memo carries an ⚠️ AI-preliminary-research disclaimer + a post-consult addendum template so the CA's actual positions can be captured verbatim after the call. Planning layer only — zero code deltas. Task #2 stays `in_progress` (research complete, awaiting the actual CA call). Earlier today: **MARGIN + REVENUE_LEAK docs refreshed for Paddle MoR era**. Post-D4, the PayPal-era assumptions baked into the v1/v2 margin audit and the 16-category revenue-leak register needed reconciling with the new processor stack (Razorpay INR + Paddle MoR). Rather than rewrite 45 inline PayPal references across two canonical docs (risking edit drift), added targeted v3 addenda:
- `docs/ai/MARGIN_VERIFICATION.md` §12 "v3 update — Paddle MoR replaces PayPal" — 8 subsections: (12.1) cost-model deltas across 6 line items (processor fee, chargeback absorption, sales-tax compliance, refund behavior, FX spread, cash settlement cadence); (12.2) recomputed processor-drag table at the new PADDLE_DEFAULT 40% INR / 60% Paddle mix showing Starter 8.7% / Creator 5.0% / Pro 4.1% / Studio 3.9% — 0.2-0.4pp worse than PayPal era on gross fee; (12.3) refreshed S1/S5/S6/S7 scenarios from §9 — S1 cheap-routing baseline shifts to Starter 88.5% / Creator 90.8% / Pro 90.2% / Studio 89.3%, S5 chargeback drag collapses on the Paddle slice (Starter gets +2.0pp, others +0.1-0.6pp), S6 region mix Paddle-variant table, S7 support-cost bomb unchanged at Starter 57.9% under $1.50 avg support; (12.4) four compliance dividends (US 50-state nexus, EU VAT OSS, chargeback liability shift, customer refund handling) newly enabled by Paddle — decisive for solo founder even if margin numbers only shift 0.2-0.4pp; (12.5) four new risks (Paddle platform risk, reserve inflation, FX spread, subscription constraints); (12.6) refreshed DoD checklist adding 3 Paddle-specific items (sandbox validation, webhook HMAC test, 30-day processor-drag reconciliation); (12.7) four immediate action items extracted; (12.8) cross-refs to MOR_EVALUATION.md + GEO_LAUNCH_POLICY.md + MASTER_PLAN.md §4 + margin_scenarios.py.
- `docs/ai/REVENUE_LEAK_AUDIT.md` §11 "v3 addendum — Paddle MoR replaces PayPal" — 9 subsections: (11.1) 9-row substitution table mapping every PayPal reference in §1-§10 to its Paddle equivalent (webhook event names, fee structure, payout currency, dispute handling, refund rails, indirect tax, FX risk, reconciliation source, risk engine); (11.2) four leak categories that collapse post-Paddle (L-5.2 EU VAT, L-6.4 chargeback evidence, L-8.5 dispute fees, L-6.1 refund fee — partial); (11.3) six newly-surfaced leaks L-11.1 through L-11.6 (Paddle account termination P1, reserve hold inflation P2, invoice-currency mismatch P1, webhook HMAC rotation P0, tax-remittance mismatch P0, FIRC paperwork gap P1); (11.4) three new ledger invariants for the Paddle slice; (11.5) six new T-L test cases T-L16-21; (11.6) four new monitors M-11.1 through M-11.4; (11.7) extended reconciliation pipeline with Paddle CSV ingest; (11.8) four new CA open questions Q11-Q14; (11.9) decision log entry noting §1-§10 retained as historical.
- `docs/ai/AI_API_MASTER_PLAN.md` §8 Gap C updated inline — PayPal "knife-edge" rewritten for Paddle economics; action updated to "Starter routes to Razorpay when buyer in IN, Paddle otherwise," test reference updated to §12.3 S6 Paddle-variant table.
- `docs/ai/margin_scenarios.py` — added `paddle` scheme to `processor_fee()` with 5% + $0.50 rate and noted no separate chargeback line needed; added 4 new PADDLE_* mix constants (PADDLE_DEFAULT 40/60, PADDLE_INDIA_HEAVY 70/30, PADDLE_INTL_HEAVY 20/80, PADDLE_ONLY 100); legacy DEFAULT_MIX / INDIA_HEAVY / US_HEAVY marked as v1/v2 retained for historical comparison.
- `MODELS_AND_MULTI_KEY.md` grepped clean — no PayPal references found, no changes needed.
All four doc changes are planning-layer only (zero code deltas, zero runtime impact). **Same-day gap-close `fbe5fe9`** closes 4 of 7 gaps surfaced in the post-v3-refresh review: (i) `lib/pricing.ts` `CreditPack.margin` field now has a comprehensive JSDoc docstring explaining what the number represents (AI-cost only under cheap routing), what it excludes (Razorpay 2%×1.18 + Paddle 5%+$0.50 processor fees, support cost, refund drag, FX spread, GST/income tax), post-Paddle net margins from §12.3 S1 (Starter 88.5% / Creator 90.8% / Pro 90.2% / Studio 89.3%), and the "up to" pricing-copy gate requirement until 7 consecutive green days — prevents the next reader from confusing headline claim with realistic net margin; (ii) `docs/ai/margin_scenarios.py` added `FX_SPREAD_PADDLE = 0.005` constant + `fx_drag_usd(price, mix)` helper applying 0.5% AD-bank retail spread to paddle + razorpay_usd + paypal slices (razorpay_inr is INR-native, zero drag); `MARGIN_VERIFICATION.md §12.2` table gained a "+ FX drag" column showing final v3 Total drag = 9.0% / 5.3% / 4.4% / 4.2% for Starter/Creator/Pro/Studio; `§12.5` point 3 updated from "currently ignored" to reference the helper + Task #4's 30-day reconciliation + fallback to Paddle INR direct payout (1% cap) if AD-bank spread trends > 0.7%; (iii) `MARGIN_VERIFICATION.md §12.4` point 4 refined with an S7-Paddle-refund-absorption sub-table showing Paddle absorbing refund tickets on the 60% intl slice lifts S7 by +1.4pp Starter / +0.3pp Creator / +0.1pp Pro / +0.0pp Studio — not enough to rescue Starter, D1 ($5→$7) remains the fix; (iv) `docs/payments/PAYMENT_GATEWAY_PLAN.md` gained a prominent SUPERSEDED banner at the top documenting D4 closure, what's still valid (Razorpay rail, portable architecture abstraction), what's obsolete (all PayPal sections, per-pack processor policy, old dispute workflow), and where to go for current guidance (MOR_EVALUATION.md, GEO_LAUNCH_POLICY.md, MARGIN_VERIFICATION.md §12, REVENUE_LEAK_AUDIT.md §11, MASTER_PLAN.md §4). `MODELS_AND_MULTI_KEY.md` verified clean — zero processor-fee assumptions to update (grep for processor/paypal/paddle/razorpay/drag returned only per-op AI margin + BYOK near-100% claim). Remaining gaps rolled into expanded Task #4 scope: PADDLE_* Hostinger env vars (blocked on Task #1), inline PayPal refs in REVENUE_LEAK_AUDIT §1-§10 (kept for git-diff readability, flagged as post-launch doc-debt sweep), L-11.5 ledger invariant (blocked on payments_ledger schema from Phase A1), 30-day processor-drag reconciliation post-launch. Paper trail update `894a84e` records `a5d4a71` + `fbe5fe9`. **Earlier 2026-04-20 Phase 0 foundation docs shipped** — four new planning docs close D4, D10, D11 founder decisions in one commit: (1) `docs/payments/MOR_EVALUATION.md` picks Paddle over Lemon Squeezy / FastSpring / Paypro Global / Gumroad / DodoPayments as the international Merchant of Record, based on weighted scoring across India-payout reliability, tax jurisdiction coverage, subscription feature maturity, and chargeback handling. PayPal dropped from the plan; Razorpay remains the IN rail. Includes 6-vendor scoring matrix, 4-hour Paddle sandbox validation checklist, and Plan B (Paypro) / Plan C (Lemon Squeezy + Payoneer) fallbacks. (2) `docs/GEO_LAUNCH_POLICY.md` formalizes the 3-tier launch policy — Tier 1 allow (IN/US/UK/CA/AU/NZ/SG/UAE + rest of APAC/LatAm/MEA, all via Razorpay on IN and Paddle on RoW), Tier 2 defer (EU 27 + CH/NO/IS/LI + CN/RU/BY — block checkout with "Notify me" form pending EU MRR threshold at $5k), Tier 3 block (OFAC-sanctioned IR/SY/KP/CU + Crimea/Donetsk/Luhansk via Cloudflare WAF 451). Includes TypeScript router stub, ~25-country allowlist, revenue-mix projection table feeding TAX_MODEL, and EU/US compliance prep queue. (3) `docs/india/TAX_MODEL.md` — 343-line CA-ready income tax model. Three-regime comparison (44ADA presumptive 50% / 44AD presumptive 6% digital / regular books). Net take-home scenarios at $50k ARR (all regimes work → 44ADA wins at ~87% margin), $100k ARR (must leave 44ADA cap — regular books path ~81% margin / 44AD aggressive path flagged as scrutiny risk), $250k ARR (LLP vs Pvt Ltd trade-off). Multi-year regime-transition roadmap Y1-Y4. Non-tax leakages quantified (FX drag 1-2%, CA fees ₹40-80k/yr, advance-tax quarterly interest, professional tax). 7 CA-confirmation questions covering 44ADA applicability to productized SaaS, Paddle export-of-services classification under s. 2(6) IGST Act, LLP/Pvt Ltd timing, DTAA with UK, advance-tax schedule, GST threshold applicability, and professional-tax enrollment. Best case 80.5% effective margin, realistic 72.7%, worst case (44AD reclassified on scrutiny) 0% or negative. (4) `docs/india/GST_SETUP.md` — 367-line operations runbook. Explains why GST applies from day 1 regardless of ₹20L threshold (inter-state supply rule, Section 24(i) CGST Act + OIDAR classification + Razorpay contract terms). Covers PAN-linked individual registration (no company needed), GST REG-01 Part A/B walkthrough with Aadhaar e-KYC fast-path, LUT filing under Form RFD-11 for zero-rated export treatment with annual renewal calendar, HSN/SAC code selection (998313 default pending CA confirmation vs 998314 / 998319 alternatives), invoice templates for IN-Razorpay B2C CGST/SGST/IGST split and USD-Paddle export-of-services zero-rated with FIRC reference, QRMP quarterly-return option, monthly calendar for GSTR-1/3B/9, reverse-charge mechanism on foreign supplier invoices (OpenAI/Anthropic/AWS/Cloudflare etc), year-end CA artefact checklist (14 items), 5 edge cases (B2B GSTIN capture, refund credit notes, chargeback handling, FX fluctuation, state move). Also MASTER_PLAN.md bumped to v4 — D4/D10/D11 marked CLOSED in §4 decisions table; new §2.2 Payments layer now tops with MOR_EVALUATION.md above PAYMENT_GATEWAY_PLAN.md (which is marked as superseded for the international half); new §2.2a India compliance subsection; new §2.2b Geographic launch policy subsection; §1 "Critical unknowns" count dropped from 12 open decisions to 9; T2-G1 (India GST) and T2-G2 (EU VAT) in PLAN_GAP_ANALYSIS now have documented remediation plans. Paper trail §8 updated with new commit SHAs once pushed. Zero code deltas — pure planning layer. These four docs together give a CA a day-1-ready package to review and sign off on before Phase 0 legal work starts. Earlier today: **SEV-2 per-page OG/Twitter CLOSED**: every non-home public route had been shipping the homepage's generic `og:title` / `og:description` on Twitter/LinkedIn/Slack share cards because Next 14 App Router shallow-merges metadata and child pages were only setting top-level `title` / `description`, so they inherited the root layout's `openGraph.title` + `openGraph.description` verbatim. Two-part fix: (1) root `app/layout.tsx` now declares `openGraph.title` + `twitter.title` as `{ default, template: "%s · pdfcraft ai" }` objects so every child page's `title` automatically propagates to social cards; (2) new `lib/page-metadata.ts` helper (`pageMetadata()` + `PageMetadataInput` type) keeps `title` / `description` / `openGraph` / `twitter` / `alternates.canonical` in sync in one line — picked up by `/about`, `/contact`, `/gdpr`, `/careers`, `/status`, `/changelog`, `/bulk`, `/macros`. The remaining public pages (`/help`, `/tools`, `/privacy`, `/security`, `/terms`, `/dpa`, `/blog` + `/blog/[slug]`, `/translate-pdf`, `/split-pdf`, `/merge-pdf`, `/pdf-to-word`, `/compress-pdf`, `/agent`, `/studio`, `/tool/[id]`) got matching `twitter` blocks appended alongside their existing `openGraph` blocks; `/help/[slug]`'s pre-existing `twitter.card: "summary"` was bumped to `"summary_large_image"` for site-wide consistency. Auth pages (`/login`, `/register`, `/signup`, `/forgot-password`, `/account`) are `robots: { index: false, follow: false }` and intentionally rely on the root-layout template (`Sign in · pdfcraft ai` etc) — no per-page override needed. `npx tsc --noEmit` exit 0 across 28 touched files. Commit `d035635` pushed to `main`; Hostinger auto-deploy in flight. Earlier today: **SEV-1 CSP strip CLOSED**: LiteSpeed/Passenger was reducing the multi-directive `Content-Security-Policy` header Node.js was setting down to just `upgrade-insecure-requests` in production — the full policy defined in `next.config.mjs` was being dropped silently between the Node runtime and the client. Fix enforced at the Apache layer: added `<IfModule mod_headers.c>` block to `/home/u692382124/domains/pdfcraftai.com/public_html/.htaccess` with `Header always unset Content-Security-Policy` followed by `Header always set Content-Security-Policy "<full policy>"`. Pre-change `.htaccess` backed up to `.htaccess.bak-pre-csp-20260420T084118Z` on the origin. Also widened `next.config.mjs` ANALYTICS_ORIGINS into three categorized arrays (`_SCRIPT` / `_CONNECT` / `_IMG`) covering GTM + Clarity + GA4 + c.clarity.ms so the origin-configured CSP matches edge-enforced byte-for-byte — belt-and-braces. Live verify: `curl -sI "https://pdfcraftai.com/?_csp_probe=…"` now returns the full CSP with every directive intact (`default-src 'self'; script-src … https://www.googletagmanager.com https://www.clarity.ms; … frame-src 'self' <razorpay+paypal>; connect-src … https://www.google-analytics.com https://c.clarity.ms …; frame-ancestors 'none'; upgrade-insecure-requests`). PCI SAQ-A scope lockdown promised in the `next.config.mjs` header comments is real in production again. Earlier: Phase 5.6 **COMPLETE**: `/tool/ai-sign` shipped end-to-end — **fifth and final** of the 5 Phase 5.6 tools. AI fills printed form fields (Name / Date / Signature / Email / Phone / Title / Company / Address / custom fields) from a user-supplied personal-info bundle (fullName required; initials, email, phone, date, company, title, address, custom k/v pairs all optional). Pipeline: (1) `lib/ai/pdf-extract.ts` -> positioned pdfjs extraction; (2) strict JSON-envelope prompt (temperature 0.1) asks the model for `{ fills: [{ label, anchorText, fieldKey, page, reason }] }` with dedicated `SignParseError` class; (3) whitespace-tolerant substring matching locates each anchor across pdfjs-extracted positioned items; (4) `pdf-lib` `drawText` right-of-anchor with binary-search shrink-to-fit + ellipsize. Signature fields use `HelveticaBoldOblique` 16pt in dark blue `rgb(0.04, 0.07, 0.42)`; regular values use `Helvetica` 10pt black. Special field keys: `__signature`, `__initials`, `__skip`, plus `custom:<slug>`. **10-credit flat cost.** Response carries the filled PDF as base64 + a markdown summary that's persisted to `ai_outputs` (kind: `"signing"`); no binary PDF storage — replays return `signedPdfBase64: null`. 25 MB PDF cap + 16 KB info cap (413 `info_too_large`). 422 `no_extractable_text` guard when >=90% of pages look OCR-candidate. Route error map: `NoAIProviderConfiguredError` -> 503; `SignParseError` -> 502 `sign_parse_failed` + refund; `pdf_extract_failed` -> 400; generic -> 502 `sign_failed` + refund; 207 persist-failed fallback still hands back the base64 PDF so the 10-credit spend isn't lost. **Explicit UI caveat**: amber-bordered "Not a cryptographic signature" card directs legally-binding e-signature needs to DocuSign / Adobe Sign. Commit `5e85ca0`. Live prod verified: `/tool/ai-sign` 200, `POST /api/ai/sign` 401 `not_authenticated` on anon multipart **and** JSON bodies, `GET /api/ai/sign` 405 (POST-only). **All 5 Phase 5.6 tools now shipped: `rewrite` (3cr) / `table` (3cr) / `redact` (5cr) / `generate` (20cr) / `sign` (10cr).** Earlier: `/tool/ai-generate` shipped end-to-end — **fourth of the 5 Phase 5.6 tools**. Prompt-to-PDF with 7 document types (memo / report / brief / letter / blog / outline / other), 3 lengths (short ~400w / medium ~1000w / long ~2000w), 4 tones (neutral / formal / casual / technical). Two-stage pipeline: (1) the model emits clean markdown at temperature 0.4, (2) a custom 620-line markdown→pdf-lib renderer lays it out on US-Letter pages using the Helvetica family (Helvetica / Bold / Oblique / BoldOblique), with block-level parsing for H1/H2/H3 + paragraphs + bullet + numbered lists + horizontal rules, inline bold/italic span tracking with word-wrap preservation across line breaks, and `ensureSpace()` cursor-based pagination. 16,000 char input cap (413 `prompt_too_long`) with `PROMPT_CHAR_BUDGET = 8,000` model-side clamp; per-length `MAX_TOKENS_BY_LENGTH` output cap (short 900 / medium 2200 / long 4600) with stop-reason truncation detection. 20-credit flat cost. Response carries the generated PDF as base64 + a markdown summary that's persisted to `ai_outputs` (kind: `"generation"`); no binary storage infra — replays return `pdfBase64: null` with a client-side "Re-run for a fresh PDF" callout. Strict JSON body at `/api/ai/generate` (not multipart — no PDF upload for this tool). Commit `d00ee8a`. Live prod verified: `/tool/ai-generate` 200 with "Generate PDF — 20 credits" button + "Describe what you want" prompt label + Length selector rendered, `POST /api/ai/generate` 401 `not_authenticated` on anon, `GET /api/ai/generate` 405 (POST-only). Earlier: `/tool/ai-redact` shipped end-to-end — third of the 5 Phase 5.6 tools. Detects 12 PII categories (email / phone / SSN / credit card / address / person name / DOB / passport / driver license / IP / API key / bank account / other), runs whitespace-tolerant substring matching across pdfjs-extracted positioned items, draws black `pdf-lib` rectangles with ±2px descender padding. Strict JSON-envelope prompt (temperature 0.1) with `RedactParseError` → 502 + refund on bad model output. 5-credit flat cost. Response carries the redacted PDF as base64 + a markdown summary that's persisted to `ai_outputs` (kind: "redaction"). **Explicit security caveat in the UI**: visual overlay only — the underlying content-stream text is untouched, so print-to-PDF is required for truly sensitive handoffs. Commit `77278b1`. Live prod verified: tool page 200 with "Redact PDF — 5 credits" button + "Drop a PDF to redact" dropzone, API 401 on anon multipart POST, GET 405 (POST-only). Earlier: `/tool/ai-table` shipped end-to-end — second of the 5 Phase 5.6 tools. Extracts tables as markdown (GFM pipe tables) **and** per-table CSV (RFC 4180). Strict JSON-envelope prompt (temperature 0.1) with `TableParseError` → 502 + refund on bad model output. 3-credit flat cost. Client runner shows a per-table card with Copy CSV / Download .csv buttons alongside a markdown preview. Commit `5205ae1`. Live prod verified: tool page 200 with "Extract tables — 3 credits" button + "Drop a PDF to extract tables" dropzone label, API 401 on anon multipart POST, stale-worker kick per CLAUDE.md §5 (pkill next-server) cleared the post-deploy 500s. Earlier: `/tool/ai-rewrite` shipped end-to-end — 5 modes (simplify/formal/casual/concise/expand), 3-credit flat cost, same auth-gate pattern as the 4 previously-live AI tools. Single migration `0003_extend_ai_output_kinds.sql` extends `ai_outputs.kind` enum to cover all 5 Phase 5.6 tools at once so the DB is ready for ai-redact / ai-generate / ai-sign without another ALTER. Commit `a5f6f52`. Live prod verified: tool page 200 + all 5 mode labels rendered, API 401 on anon POST, DB enum has 9 values. Earlier context: Post-`/app/studio`-deletion logic audit follow-ups: (a) all four AI tool runners — Summarize / Translate / OCR / Compare — now `router.push("/login?callbackUrl=/tool/ai-<id>")` on a 401 from `/api/ai/*` instead of showing a dead-end "Sign in to…" error card; (b) new `app/app/studio/page.tsx` redirects to `/app/dashboard` so old bookmarks survive the middleware-driven login bounce. Commit `7b6a381`. Earlier this day: removed authenticated `/app/studio` Batch/Smart runner — public `/agent` + `/studio` + per-tool pages now own that surface. Dropped `agent_runs` + `agent_run_steps` tables; `userMacros` preserved. Agent/Macros/Studio design audit + ship — `/agent` rebuilt as interactive plan→review→run surface, new `/macros` library with mini-SVG previews, brand-new `/studio` full-bleed canvas editor — all three ported from the Claude Design handoff bundle. `tsc --noEmit` clean. Production readiness audit also completed — see `PRODUCTION_AUDIT_2026-04-20.md`. Pending SEV-1 CSP strip + SEV-2 per-page OG override + SEV-3 canonical/twitter hygiene items tracked below.)

---

## How to use this file

- Anything in **Done** is verified live — don't redo it.
- Anything in **Pending (Claude can do)** is fully automatable from a Cowork session — pick it up.
- Anything in **Pending (needs the user)** requires a human action (DNS console, mailbox check, paid signup, etc.) — don't guess credentials, just remind the user.
- When you finish an item, move it to Done with the date and the verification command/screenshot.

---

## Done

### Infra

- [x] **Cloudflare proxy in front of Hostinger** — verified via `cf-ray` + `server: cloudflare` on every response. (2026-04-19)
- [x] **Apex + www both serve the app** — both resolve, www redirects to apex. (2026-04-19)
- [x] **`robots.txt` advertises sitemap** — `Sitemap: https://pdfcraftai.com/sitemap.xml`. (2026-04-19)
- [x] **`sitemap.xml` returns 200, application/xml, 39 URLs.** (2026-04-19)

### Auth

- [x] **NextAuth v5 wired to Google.** Env vars `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `NEXTAUTH_URL`, `NEXTAUTH_SECRET` all set in Hostinger. (2026-04-19)
- [x] **`trustHost: true` on the NextAuth config** — fixes Cloudflare-proxy → Next.js host trust issue. Commit `ffdfde5`. (2026-04-19)
- [x] **Google OAuth consent screen verified.** All three brand URLs (Home, Privacy, Terms), 120×120 logo, support email, authorised domain `pdfcraftai.com`. Filled via Chrome MCP. (2026-04-19)
- [x] **`/api/auth/providers` exposes Google with the correct apex callback URL.** (2026-04-20)

### Analytics

- [x] **GA4 (`G-2Y8PS0S93F`) tag added to `app/layout.tsx`, committed in `36034eb`, deployed, verified rendering.** (2026-04-20)
- [x] **Microsoft Clarity (`wcsbv536zv`) tag added to `app/layout.tsx`, committed in `36034eb`, deployed, verified rendering.** (2026-04-20)

### Auth UX (sitewide)

- [x] **TopNav rewrite: session-aware actions, avatar image (Google profile photo via `next/image` `unoptimized`), click-outside + Escape close, mobile hamburger menu.** (2026-04-20)
- [x] **Middleware (`auth.config.ts`) redirects logged-in users away from `/login`, `/register`, `/signup`, `/forgot-password`.** (2026-04-20)
- [x] **`MarketingChrome` hides marketing TopNav/Footer on all auth pages exactly.** (2026-04-20)
- [x] **Shared `AuthShell` + `AuthBits` design tokens across Login / Register / Signup / Forgot Password.** (2026-04-20)
- [x] **Session-aware CTAs (`SmartCta`) on the homepage final CTA and the pricing hero.** (2026-04-20)

### Marketing routes (filled 404s from footer + nav)

- [x] `/agent` — Agent mode landing. (2026-04-20)
- [x] `/macros` — Workflow Studio landing. (2026-04-20)
- [x] `/bulk` — Batch-processing landing. (2026-04-20)
- [x] `/about` — Mission + values. (2026-04-20)
- [x] `/contact` — Client form + mailto cards, posts to `/api/contact`. (2026-04-20)
- [x] `/changelog` — Release notes, tagged. (2026-04-20)
- [x] `/status` — Service health board with incident log. (2026-04-20)
- [x] `/careers` — Values + open-roles empty state w/ intro mailto. (2026-04-20)
- [x] `/gdpr` — Rights, data categories, sub-processors, DPO contact. (2026-04-20)

### Free tools (client-side WASM)

- [x] **`/tool/page-numbers` runner live.** New `PageNumbersTool` component ships two modes in one runner: (a) **Page numbers** — 4 formats (`1`, `1 / N`, `Page 1`, `Page 1 of N`) × 6 positions (TL/TC/TR/BL/BC/BR), adjustable font size 8–24pt; (b) **Watermark** — user-entered text (≤40 chars) drawn diagonally at 45° across page center, adjustable font size 24–96pt, adjustable opacity 5–60%. Built on `pdf-lib` with `StandardFonts.Helvetica` / `HelveticaBold`; fully client-side, no server round-trip. Wired into `LIVE_TOOL_IDS` in `app/tool/[id]/page.tsx`. Flips T5 in TEST_PLAN.md from Pending → Ready-to-test. (2026-04-20)
- [x] **`/tool/to-pdf` runner live.** New `ImageToPdfTool` component accepts JPG + PNG (≤20 MB each), embeds each image as a page, and saves one PDF. Supports three layout modes: fit-to-image (each page sized to source dimensions, capped at 3000pt), US Letter with centered fit, A4 with centered fit. Adjustable page margin (0–72pt) in Letter/A4 modes. Multi-file reorder + remove controls mirror MergePdfTool. Built on `pdf-lib` `embedJpg` / `embedPng`. Brings the live free-tool count to six. (2026-04-20)
- [x] **`/tool/protect` runner live.** New `ProtectPdfTool` component ships two modes in one runner: (a) **Protect** — set user password (≥ 4 chars, confirm-field enforced), optional separate owner password, per-permission grid (allow printing / copying text / editing / annotations); (b) **Unlock** — provide current password, strip the encryption on save so the output PDF is decrypted. Auto-detects whether the dropped file is already encrypted and flips the default mode. Built on `@cantoo/pdf-lib`, a maintained pdf-lib fork that adds the PDF encryption handler (pdf-lib 1.17 can read but not write encrypted PDFs). `@cantoo/pdf-lib` is dynamic-imported only on this runner — the other free-tool bundles are unaffected. Fully client-side — the password never leaves the browser; same "Stays in your browser" reassurance card as the other free tools. Also fixed a Next 14 prerender break: added a `<Suspense>` boundary around `<LoginForm>` in `app/login/page.tsx` because `useSearchParams()` (for the `?reset=1` flash added with the reset-redemption flow) requires one for static generation. Brings the live free-tool count to seven. (2026-04-20)
- [x] **`/tool/rotate` upgraded to true "Rotate & Reorder".** Previous runner only rotated. New `RotatePdfTool` is a single-screen flow with three operations: (a) bulk row — "Rotate all 90° CW", "Rotate all 180°", "Rotate all 90° CCW", "Reverse order", "Undo all edits"; (b) per-page row — show page index + "was #N" badge when reordered + accumulated rotation chip + ↑ / ↓ / rotate-CW / rotate-CCW / delete buttons; (c) Apply & download builds the output via `copyPages` in the edited order, applying each page's accumulated rotation. Output filename suffix is chosen per edit kind (`-rotated`, `-reordered`, `-edited`). Edits stored as a delta from source so "Undo all edits" reloads the file cleanly. Lives up to the registry's "Rotate & Reorder" / "Fix orientation and rearrange pages" promise. (2026-04-20)

### API / monitoring

- [x] **`/api/health` endpoint shipped.** Pings the DB with `SELECT 1`, returns `{ ok, service, commit, uptimeSec, db: { ok, latencyMs|error }, ts }`. Status code 200 on healthy / 503 on DB failure. `cache-control: no-store` so Cloudflare never serves a stale probe. Consumed by the status page and safe to bind as the Cloudflare origin health check. Error strings sanitized — never echoes DSN fragments. **Verified live 2026-04-20 01:50 UTC: `{ "ok": true, "db": { "ok": true, "latencyMs": 48 }}`.** (2026-04-20)

### Deploy recovery

- [x] **Hostinger deploy unstuck — SmartCta RSC serialization bug fixed.** Hostinger's Next build had been failing silently since commit `6941953` because the new `SmartCta` client component accepted `children` as a render-prop function `(label) => ReactNode`, and both consumers (`app/pricing/page.tsx`, `components/landing/LandingSections.tsx` FinalCTA) are Server Components — passing a closure across the Server→Client boundary tripped `Error: Functions cannot be passed directly to Client Components` and timed out static generation of `/`. Diagnosed via SSH-fetched deploy log `~/public_html/.builds/logs/.../2026-04-19_21-23-38_deploy.log`. Fix in commit `322e55b`: replaced render-prop with serialisable `iconBefore` / `iconAfter` slots, updated both call-sites, added a header comment so future hands don't reintroduce the pattern. Deploy `BUILD_ID` flipped `NgDx0xN2ZJtuJfWWuVLl7` → `P5OkoPR8soHHEdoZBCyWi`; `/api/health`, `/api/contact`, `/api/auth/forgot-password`, `/forgot-password`, `/tool/page-numbers`, `/tool/to-pdf`, `/tool/rotate` all verified 200 in prod. Four commits' worth of features unblocked in one push. (2026-04-20)

### Legal content audit

- [x] **Privacy + Terms stale-contact audit complete.** Both pages render through `lib/legal-docs.ts` which uses a single `SUPPORT_EMAIL = "support@pdfcraftai.com"` constant. No stray addresses on either page; when the mailbox goes live, the consistent constant makes the cutover a single-line change. (2026-04-20)

### Paddle MoR signup (Merchant of Record for international rail)

- [x] **Paddle vendor account created — Seller ID `320957`.** Signed up 2026-04-21 under `rajasekarjavaee@gmail.com` (India / Proprietorship / PDFCraft AI trading name). All 7 onboarding screens completed: business details Part 1 + 2 (address, contact), account type selection (Proprietorship), product description + AUP, website verification (5 URLs: `pdfcraftai.com`, `/pricing`, `/terms`, `/privacy`, `/refund-policy`), business KYC (age of business 2026-04-16 per Hostinger invoice `H_41528083`, trading name, business address), compliance screening (4/4 risk questions answered — not on MATCH list, no prior PSP terminations, no regulator actions). Verification status: **In progress** (Paddle's documented SLA is 3–7 business days). Sandbox is available immediately — production checkout paths are gated behind verification completion. Seller ID recorded in `CLAUDE.md §3`. (2026-04-21)
- [x] **Paddle adapter scaffolded at `lib/payments/adapters/paddle.ts` (681 lines, scaffold status).** Implements the `PaymentProvider` contract: `createCheckout` for both one-time (pack) and subscription (plan) via Paddle's `POST /transactions` endpoint with `custom_data.internalPaymentId` as the portability anchor; `verifyWebhook` with `paddle-signature: ts=...;h1=...` HMAC-SHA256 verification + 10-minute replay window + 2-minute future-drift tolerance; `normalize` for 10 Paddle event types (`transaction.completed`/`paid`/`payment_failed`, `adjustment.created`/`updated` for refunds, 6 `subscription.*` lifecycle states) mapping to the existing 5-kind `NormalizedPaymentEvent` union; `refundByProviderRef` via `POST /adjustments` with item-level resolution (partialRefunds capability flag off until sandbox-validated); `listTransactionsSince` async generator with cursor pagination via `meta.pagination.next` URL extraction for the nightly reconciliation cron. Sandbox vs live host selection via `PADDLE_ENV`. All live-API call sites tagged with `TODO(paddle-sandbox)` markers to flag doc-drift risk until hands-on sandbox validation confirms payload shapes. Wired into `lib/payments/registry.ts` (fourth adapter row) with five-env-var gate (`PADDLE_API_KEY` + `PADDLE_CLIENT_TOKEN` + `PADDLE_WEBHOOK_SECRET` + `PADDLE_SELLER_ID` + optional `PADDLE_ENV`). Webhook route shipped at `app/api/webhooks/paddle/route.ts` mirroring the Razorpay route shape. Secrets template updated with a dedicated `--- Paddle MoR ---` section including sandbox-vs-live key-prefix hints. `npx tsc --noEmit` exit 0 across the whole repo. (2026-04-21)
- [x] **Privacy + Terms + DPA updated for Paddle KYC verification crawl.** Paddle's verification team crawls `/privacy` and `/terms` during the 3-7 business day KYC review — a missing merchant-of-record acknowledgment and/or a missing named-sub-processor list is a well-documented Paddle verification-fail pattern. Proactive fixes in `lib/legal-docs.ts` (single source for all 7 legal pages): **(a) /privacy** — bumped updated date to 2026-04-21; replaced the now-false "No third-party trackers" line with accurate language naming GA4 + Microsoft Clarity configured with IP anonymization + no advertising identifiers; added a new "Payments and merchant of record" section stating Razorpay for IN / Paddle for international with explicit "seller of record" + "collecting and remitting sales tax / VAT / GST" language + card-data-never-touches-us PCI disclaimer; added a new "Sub-processors" section naming all six entities (Hostinger International Ltd, Cloudflare Inc, Google LLC, Microsoft Corporation, Razorpay Software Pvt. Ltd., Paddle.com Market Ltd) with country of operation + data-category + 30-day change-notice commitment; added a new "International transfers" section referencing EU SCCs + UK IDTA + Paddle's own SCCs flow-through. **(b) /terms** — bumped updated date to 2026-04-21; inserted a new "Merchant of record (international customers)" section explicitly naming Paddle.com Market Ltd (UK) + Paddle.com Inc. (US) as MoR for non-IN customers, clarifying Paddle's tax-remittance responsibility, noting that Paddle's Buyer Terms govern the purchase transaction while these Terms govern service use, and reassuring IN customers that Razorpay route is unchanged. **(c) /dpa** — bumped updated date to 2026-04-21; dropped the "Working draft" disclaimer since the sub-processor list is now concrete; replaced the vague "small number of vetted subprocessors…available on request" language with the same six named entities; extended the "International transfers" section with the Paddle-SCC flow-through. `npx tsc --noEmit` exit 0 confirms the strict `LegalDoc` shape is preserved. Zero runtime changes — pure content edits that cascade through all 7 legal routes via the existing `LegalPage` component. (2026-04-21)

### Razorpay pre-application compliance (merchant-onboarding readiness)

- [x] **Full audit written** — `docs/RAZORPAY_READINESS.md`. 7-gap checklist against Razorpay's public-website requirements for an Indian individual merchant. Every blocker closed; optional items flagged; info-needed-from-user section answered (business type: individual account, registered address: No. 311 3rd Cross Street, Eswari Nagar, Chromepet, Chennai 600044, phone: +91 94984 98011, operator name: Rajasekar Selvam, PAN handled at Razorpay dashboard KYC not on public site). (2026-04-20)
- [x] **Three new policy pages live** — shipped in commit `d201214` ("feat(legal): add refund/cancellation/shipping pages for Razorpay compliance"), deployed as `d201214e0440`:
  - `/refund-policy` — 7 sections: credit packs (14-day refund window), bonus credits (non-refundable), how to request, how refunds are returned (original method, 5–10 business days), failed/duplicate payments, chargebacks, contact.
  - `/cancellation-policy` — 5 sections: credit packs (no ongoing obligation), subscriptions (cancel anytime, no proration), account deletion, cancellation by us (policy violation / abuse), contact.
  - `/shipping-policy` — 6 sections: digital service (no physical shipping), credit delivery timeline (typically under 1 minute; 99th-percentile under 5 minutes), what to do if credits don't appear, service availability (best-effort SLA + status page link), geographic availability (global with OFAC carveouts), contact.
  - All three wired through the existing `lib/legal-docs.ts` registry (`LegalSlug` union expanded 4→7) so they automatically appear in `LEGAL_SLUGS`, `app/sitemap.ts`, and the in-page nav pills in `components/marketing/LegalPage.tsx`. Each `app/<slug>/page.tsx` exports proper `metadata` with `alternates.canonical`, `openGraph`, and `twitter` blocks — consistent with the SEV-2 per-page-metadata pattern. (2026-04-20)
- [x] **Footer legal column expanded** — `components/nav/Footer.tsx` now lists `Refund Policy`, `Cancellation`, `Shipping & Delivery` between Terms and Security. (2026-04-20)
- [x] **Registered-office address + phone now live sitewide.** Footer has a new `<address>` block under the logo tagline showing "Operated by Rajasekar Selvam / No. 311, 3rd Cross Street / Eswari Nagar, Chromepet / Chennai, Tamil Nadu 600044, India / +91 94984 98011 · support@pdfcraftai.com". `/contact` has a new dedicated "REGISTERED OFFICE" card above the help-docs card in the aside column, with the same details plus a clickable `tel:+919498498011` link — the exact pattern Razorpay reviewers look for on `/contact`. (2026-04-20)
- [x] **Entity-mismatch risk removed.** Bottom-of-footer copyright changed from `© {year} PDFCRAFT AI, INC.` (implied US corporation — would contradict the Indian-individual KYC the merchant application declares) to `© {year} pdfcraft ai · Operated by Rajasekar Selvam`. Same operator line now appears in the footer address block and the Contact registered-office card, so every public page matches what the Razorpay merchant application will state. (2026-04-20)
- [x] **Privacy policy vendor-agnostic.** `lib/legal-docs.ts` §1 changed `"...processed by Stripe..."` → `"...processed by our payment gateway partner..."`. The live Privacy Policy no longer names a competing processor before the Razorpay application is submitted. (2026-04-20)
- [x] **"Working draft" disclaimer stripped from Privacy + Terms.** Both pages now present as finalized policies (kept on Security + DPA since those remain genuine works-in-progress). Commit `d201214` also added a "Cancellation" section to `/terms` linking to `/cancellation-policy`, and a "Governing law" section declaring India / Chennai courts as the forum. (2026-04-20)
- [x] **Live verification complete (post-deploy).** `/api/health` flipped from `1d02f10f92b3` to `d201214e0440` after ~3 min 15 s of Hostinger redeploy; no stale-worker kick needed. `curl -sI https://pdfcraftai.com/{refund-policy,cancellation-policy,shipping-policy}` → all 200. Live footer HTML contains `Operated by Rajasekar` + `Eswari Nagar`; `/contact` HTML contains the full address + `+91 94984 98011`; `/refund-policy` body renders `Refund Policy` heading + `14 days` language + `support@pdfcraftai`. Next step on the user's side: submit the Razorpay merchant application — during KYC inside the Razorpay dashboard they will still need to upload their PAN, a canceled cheque or bank statement, and a photo ID (those happen inside Razorpay, not on pdfcraftai.com). (2026-04-20)

### Content depth

- [x] **Help Center — 24 articles routed at `/help/[slug]`.** `lib/help-topics.ts` rewritten with 6 topics (getting-started / ai-tools / security-privacy / billing / api-developers / troubleshooting) × 4 articles each; every article ships a ~3-paragraph body. New dynamic route `app/help/[slug]/page.tsx` renders breadcrumb, topic-icon header, summary lede, paragraph body, "still stuck?" support card, related-articles list (siblings), cross-topic browse chips. `generateStaticParams` emits all 24 slugs. `generateMetadata` includes per-article OG (`type: "article"`, siteName) + Twitter summary cards + keywords. `HelpSearch` now searches `title + summary + body + topic.name`, results route to the detail page with a topic + summary preview line. Sitemap fans out `helpRoutes` (priority 0.5). Verified: `tsc --noEmit` clean; `next build` shows `/help/[slug]` generating 24 paths. (2026-04-20)
- [x] **API reference — `/api` upgraded from placeholder to real docs.** `lib/api-endpoints.ts` extended with rate-limit tiers (Sandbox / Free / Pro / Scale), rate-limit response headers, 10 error codes (400/401/402/413/415/422/429/500/503), per-endpoint request + response examples with notes for all 8 endpoints, HMAC-signed webhook receiver snippet + 4 event types, idempotency snippet. Page rebuilt with on-page nav, quickstart, auth guide (pk_live / pk_test / sk), endpoints summary table + 8 detail cards with request/response side-by-side, rate-limit tables, error code table, webhook section, idempotency section, footer CTA. Removed "Phase 2" placeholder copy. Verified: `tsc --noEmit` clean; `next build` ok. (2026-04-20)

### Theme + testing infrastructure

- [x] **Light/dark theme toggle** shipped in TopNav via new `ThemeToggle` component. Uses the existing pre-hydration script in `app/layout.tsx` + `[data-theme]` CSS variables in `globals.css`. Stored in `pdfcraft_state.theme`. (2026-04-20)
- [x] **`docs/TEST_PLAN.md` created** — P0–P6 batches (auth, free tools, AI tools, authed app, marketing/SEO, error/edge cases, a11y/perf). (2026-04-20)
- [x] **`docs/FEATURE_TRACKER.md` created** — Done / Partial / Pending matrix across marketing, auth, API, product, analytics, SEO, security. (2026-04-20)
- [x] **`scripts/test-pdf-tools.mjs` — Node-based smoke harness for every client-side PDF tool.** Drives the same `pdf-lib` / `@cantoo/pdf-lib` APIs the browser runners use, so correctness can be asserted without a real browser. 17 tests across 7 tool groups: merge (3+2+4 pages, single-PDF passthrough), split (5-page → 5 singletons), rotate & reorder (rotate, reverse, delete), compress (useObjectStreams resave stays valid), page numbers + watermark (stamp every page, diagonal overlay), image → pdf (PNG, JPG, combined), protect + unlock (encrypt adds Encrypt dict, stock pdf-lib rejects, wrong password throws, user + owner password both unlock, resave strips encryption). All 17 pass in `node scripts/test-pdf-tools.mjs`. (2026-04-20)
- [x] **`scripts/smoke-live.mjs` — Production smoke harness.** Hits the live apex and asserts `/api/health` 200 + cache-control, GET 200 on 6 marketing pages + 7 tool runners, auth redirects for unauthed app routes, `/api/auth/forgot-password` contract (400 on bad email, 200 on valid — including replay, enforcing the anti-enumeration contract), `/api/auth/reset-password` contract (400 on missing / non-hex, 409 on unknown token), `/sitemap.xml` + `/robots.txt`. 24/26 pass as of 2026-04-20 02:45 UTC; the two failures (`/account` 404, unknown-token 500 on reset-password) are fixed locally and pending deploy — see "Prod hotfixes landing in next deploy" below. (2026-04-20)

### Prod hotfixes landing in next deploy

- [x] **`/api/auth/reset-password` 500-on-every-call → fixed via self-healing schema bootstrap.** Root cause: Hostinger doesn't run `drizzle-kit push` on deploy, so the `password_reset_tokens` table that `0001_password_reset_tokens.sql` defines was never created on production MySQL. Every reset attempt threw `ER_NO_SUCH_TABLE` inside `consumePasswordResetToken` and the route catch surfaced a generic 500. Fix in `lib/password-reset.ts`: one-time-per-process `ensureSchema()` that runs `CREATE TABLE IF NOT EXISTS password_reset_tokens (...)` matching the migration byte-for-byte, called from the top of `mintPasswordResetToken` and `lookupPasswordResetToken`. Defence-in-depth: route now inspects `err.code === "ER_NO_SUCH_TABLE"` and returns 409 (same user-facing message as a stale/missing token) instead of 500. Bootstrap is marked `REMOVE WHEN MIGRATIONS RUN AT DEPLOY` so we don't leave runtime DDL in place forever. Local PDF-tool harness still 17/17 green; `tsc --noEmit` clean. (2026-04-20)
- [x] **`/account` → `/app/settings` redirect shipped.** `/account` was 404'ing in prod despite being linked from the pricing-page BYOK CTA. Added `app/account/page.tsx` that `redirect("/app/settings")` at render — keeps external links working (email campaigns, marketing collateral) without duplicating the settings UI. `/app/settings` itself already auth-gates, so unauthed visitors get bounced to `/login` transitively. (2026-04-20)
- [x] **Smoke harness anti-enumeration assertion corrected.** Initial `scripts/smoke-live.mjs` asserted the second forgot-password call should 429; the endpoint intentionally stays 200 on throttle to avoid leaking which addresses are rate-limited. Updated the test to assert 200 + document why. (2026-04-20)
- [x] **`/tool/pdf-to-office` runner shipped (user-reported defect: "not working").** Root cause: the URL returned 200 HTML, which is why `scripts/smoke-live.mjs` was passing, but the page was rendering `ComingSoonRunner` because `pdf-to-office` wasn't in the `LIVE_TOOL_IDS` set in `app/tool/[id]/page.tsx`. From an end-user's POV (including the reporter's screenshot), the disabled "Choose file / Use sample" buttons made the tool indistinguishable from broken. Fix: three new files shipped together — (1) `lib/tools-server/pdf-to-office.ts` converts extracted text to `.docx` (one heading per page, page-break between pages, Calibri 11pt, US Letter, OCR-candidate placeholder paragraphs) or `.txt` (UTF-8 with `\f` between pages); (2) `app/api/tools/pdf-to-office/route.ts` is the POST endpoint — Node runtime, 25 MB body cap, per-IP 10/min throttle, Zod format enum, status-code map (400 bad form / invalid PDF, 413 too large, 422 `no_extractable_text`, 429 rate-limited, 500 server error), Content-Disposition with RFC 5987 filename, `x-page-count` + `x-ocr-candidate-pages` response headers for the client UI; (3) `components/tools/PdfToOfficeTool.tsx` is the client runner — `ToolDropzone` + radio-card format selector (Word / Plain text) + ResultCard with download button + conditional OCR nudge + explicit v2-deferred note pointing tabular users to `/tool/ai-ocr`. Wired into `app/tool/[id]/page.tsx` switch, and that page now special-cases the reassurance row for server-side free tools so the "Stays in your browser" claim isn't a lie (new copy: "Your PDF is converted in-memory on our servers and discarded the moment the download completes — nothing is stored."). Server-side was the right call despite breaking the all-client-side free-tool pattern: pdfjs's worker needs bundling under a strict CSP and the `docx` library would bloat every visitor's bundle. No auth, no credit spend, no disk writes. `tsc --noEmit` clean, 17/17 existing PDF-tool harness still green. **Also extended `scripts/smoke-live.mjs`** with a new per-path assertion that live tool pages must NOT contain the string `TOOL RUNNER LANDS IN` — so any future regression where a runner is registered-but-disabled fails smoke instead of passing silently. Installed `docx@^9.6.1` as a runtime dep. (2026-04-20)
- [x] **`/api/tools/pdf-to-office` worker hotfix — real PDFs now round-trip end-to-end.** After the runner shipped, the page rendered correctly but the actual conversion 500'd in prod. SSH into Hostinger + tailing `~/domains/pdfcraftai.com/nodejs/console.log` revealed pdfjs's "fake worker" fallback was failing at runtime: `Setting up fake worker failed: "Cannot find module '.../pdfjs-dist/legacy/build/pdf.worker.mjs' imported from '.../pdfjs-dist/legacy/build/pdf.mjs'"`. Root cause: pdfjs's `pdf.mjs` does a *dynamic* import of `./pdf.worker.mjs` to spin up its in-process worker under Node, and that dynamic import is invisible to Next.js's standalone tracer — so the 2.3 MB worker file was never copied into `.next/standalone/node_modules/pdfjs-dist/legacy/build/`. Fix is two layers, both in this commit: **(1)** `next.config.mjs` adds `experimental.outputFileTracingIncludes` to force-copy `pdfjs-dist/legacy/build/pdf.worker.mjs` into the standalone bundle for every `/api/**/*` route — so pdfjs's default `./pdf.worker.mjs` lookup now resolves; **(2)** `lib/ai/pdf-extract.ts` adds an idempotent `ensureWorkerConfigured()` that runs on first call to `extractPdfText`, uses `createRequire(import.meta.url).resolve(...)` + `pathToFileURL(...)` to pin `GlobalWorkerOptions.workerSrc` to an explicit `file://` URL — belt-and-braces if a future Next version changes its tracing rules, and the idempotent flag means any latency hit only happens on the very first conversion in a worker's lifetime. Critically, the setter runs LAZILY inside the function (not at module load), because eagerly resolving at import-time breaks `next build`'s "Collecting page data" phase, which evaluates route modules in a webpack-bundled context where `require.resolve` against an ESM package fails before standalone tracing has even copied node_modules. Verified locally with a synthesized 3-page PDF round-tripped through the standalone bundle: `status=200`, `x-page-count=3`, 8.8 KB output, valid .docx ZIP whose `word/document.xml` contains all three pages of the input text. Also benefits `/api/ai/{chat,summarize,translate,compare,ocr}` — they share the same extractor and were silently latently broken too. **Verified LIVE on prod (commit `8271a61`, 2026-04-20 03:42 UTC):** same synthesized 3-page PDF POSTed to `https://pdfcraftai.com/api/tools/pdf-to-office` returned `status=200`, `x-page-count=3`, 8886-byte .docx, and the extracted `word/document.xml` contains all three pages of input text verbatim. Brief prod instability followed the deploy — LiteSpeed accumulated five stale `next-server` processes from prior deploys (02:59 / 03:13 / 03:31 / 03:32 / 03:39) which hit Node's thread-creation limit (`Assertion failed: (0) == (uv_thread_create(...))` in `stderr.log`) and 503'd the smoke harness; resolved via SSH `pkill -9 next-server` + `pkill -9 -f lsnode.js` so LiteSpeed could respawn one clean worker. Post-restart: `/api/health` ok=true db=31ms + full 54/54 `scripts/smoke-live.mjs` PASS + end-to-end PDF round-trip re-verified. (2026-04-20)

### Agent / Macros / Studio (Claude Design parity)

- [x] **`AGENT_MACROS_DESIGN_GAP.md` audit shipped** — compared the Claude Design handoff bundle (`agent.jsx` + `workflow.jsx` + `index.html`) against shipped pdfcraftai.com. Findings: shipped `/agent` was static marketing copy; shipped `/macros` was a feature grid with no library; `/studio` did not exist. Design specifies a plan→review→run agent flow, a templates+saved-macros library with mini SVG previews, and a full-canvas drag-and-drop workflow editor with 28 node types across 9 categories. (2026-04-20)
- [x] **`lib/workflow/{nodes,templates,demo-state,agent-plan}.ts` shared core** — single source of truth ported from the design bundle. `nodes.ts` defines `NODE_TYPES` (28 entries, color-coded per category) + `NODE_CATEGORIES` + `NODE_COLOR` swatches; `templates.ts` ships 4 seeded `MacroTemplate`s (Invoice Intake, Diligence Intake, Handbook Refresh, Contract Review) with full node + edge graphs; `demo-state.ts` brokers localStorage (`pdfcraft.demo.{credits,history,macros}`) with 1000 starting credits, history capped at 50; `agent-plan.ts` is a deterministic keyword-pattern matcher that maps a free-text prompt to an `AgentPlan` (steps + outputs + cost) — receipt/expense/invoice / redact / investor-summary / translate / study / due-diligence / generic fallback. No LLM call in the demo path. (2026-04-20)
- [x] **`/agent` rebuilt as interactive surface (`components/agent/AgentInteractive.tsx`).** State machine: `idle → planning → reviewing → running → done`. User types intent or picks an example chip → `buildPlan` returns steps + outputs + total credit cost → user reviews → "Run" walks through each step with `pulse-soft` indicator + collapsible terminal-style log. Spends credits via `spendDemoCredits`, logs the run via `addDemoHistory`. Captures `?saveAsMacro=1` URL hint to deep-link from the Macro Library "Describe to Agent" tile. Marketing prose moved INTO the component so the prompt box is the hero; `app/agent/page.tsx` reduced to a server-component metadata shell that mounts `<AgentInteractive/>` inside `<Suspense>` (required because the client uses `useSearchParams()`). (2026-04-20)
- [x] **`/macros` library shipped (`components/workflow/MacroLibrary.tsx` + `MacroCard.tsx` + `MiniPreview.tsx`).** Filter pills (`All` / `Yours` / `Community`) — community = seeded templates, yours = `getUserMacros()` from localStorage. Two featured tiles at the top: "Start from scratch" → `/studio`, "Describe to Agent" → `/agent?saveAsMacro=1`. Each card embeds a pure-SVG `MiniPreview` rendering the workflow as 40×36 rounded rects connected by bezier-curve edges, with `viewBox` auto-computed from node positions and `preserveAspectRatio="xMidYMid meet"`. Clicking a card opens `/studio?t=<id>`. Marketing feature grid + final CTA retained underneath the library for SEO and first-time-visitor explanation. (2026-04-20)
- [x] **`/studio` brand-new full-bleed canvas editor (`components/workflow/WorkflowStudio.tsx`).** ~1000-line client component. Loads template by `?t=<id>` (defaults to `tpl-invoice` if absent or unknown). Three-panel layout: left toolbox (28 node types grouped by category with color-coded chips, drag-to-add), center canvas (drag-to-position nodes, drag-from-handle-to-target-node to connect, click-to-select nodes/edges, click-to-delete edges), right inspector (type-specific config fields for ai_translate / ai_sum / ai_redact / email_out / slack / protect / watch / if_cond / schedule + universal On-error select + JSON `INPUTS` preview + delete). Bottom collapsible terminal-style RunLog with auto-scroll. `Run` walks the graph topologically — root nodes first then via edges, then orphans — with `setTimeout(850ms)` per node + 380ms gap, marking each node `running` (with `pulse-soft` halo) → `done`, spending total credits at the end via `spendDemoCredits` and logging via `addDemoHistory`. `Save as macro` calls `addUserMacro({ name, description, nodes, edges, icon: "Flow" })` and routes back to `/macros`. SVG defs for arrow markers (`ws-arrow` / `ws-arrow-active`). Cleanup of all `setTimeout` handles on unmount. Footer hidden via `MarketingChrome` so canvas takes `calc(100vh - 64px)` (TopNav stays for nav back). `app/studio/page.tsx` is a server-component metadata shell that mounts `<WorkflowStudio/>` inside `<Suspense>` (required because the client uses `useSearchParams()`). (2026-04-20)
- [x] **`MarketingChrome` updated to support no-footer routes.** New `NO_FOOTER_EXACT = new Set(["/studio"])` + `NO_FOOTER_PREFIXES = []` set. Studio keeps the TopNav for navigation but hides the Footer so the canvas can fill the viewport. (2026-04-20)
- [x] **Verification: `npx tsc --noEmit` exit 0 across the new file set** (`AgentInteractive.tsx`, `WorkflowStudio.tsx`, `MacroLibrary.tsx`, `MacroCard.tsx`, `MiniPreview.tsx`, `lib/workflow/*.ts`, all three updated `app/{agent,macros,studio}/page.tsx`, modified `MarketingChrome.tsx`). 3,044 net lines added. (2026-04-20)
- [x] **AI tool runners bounce anonymous users to `/login` on 401.** Follow-up from the post-`/app/studio`-deletion logic audit: anonymous visitors landing on `/tool/ai-summarize` (or translate / ocr / compare) from the home-page CTA could attach a PDF, click Run, and get a bare "Sign in to summarize PDFs — credits are per-user." string in an error card with no Sign-in CTA — a dead end. Fix: each of the four AI tool components (`SummarizePdfTool`, `TranslatePdfTool`, `OcrPdfTool`, `ComparePdfTool`) now imports `useRouter` from `next/navigation` and intercepts `res.status === 401` BEFORE calling `mapErrorBody`, doing `router.push("/login?callbackUrl=" + encodeURIComponent("/tool/ai-<id>"))` so post-login the callback brings the user right back to the tool. Verified live: `curl -X POST https://pdfcraftai.com/api/ai/summarize` still returns `401 {"error":"not_authenticated"}` (route contract unchanged); the redirect happens client-side. Commit `7b6a381`. (2026-04-20)
- [x] **`/app/studio` no longer 404s for users with old bookmarks.** Same audit identified that the middleware (`auth.config.ts`) still gates `/app/*` behind a session, so an anon visitor hitting `/app/studio` gets `307 → /login?callbackUrl=/app/studio` — and after sign-in the callback dropped them at a 404 because the page was deleted in the prior commit (`10b605c`). Fix: new `app/app/studio/page.tsx` is a minimal server-component that calls `redirect("/app/dashboard")`, with `metadata.robots = { index:false, follow:false }` and `dynamic = "force-static"`. Net effect: anon → /login → sign in → /app/studio → /app/dashboard (a working destination). Public `/studio` demo unaffected. Verified `curl -sI https://pdfcraftai.com/app/studio` → `307` to login. Commit `7b6a381`. (2026-04-20)
- [x] **`/tool/ai-sign` shipped end-to-end (fifth and final of 5 Phase 5.6 Coming-Soon tools — Phase 5.6 COMPLETE).** Commit `5e85ca0` (2026-04-20). Three new files + one edited wire-up: (1) `lib/ai/sign.ts` — ~700-line orchestrator. Exports `SignFieldKey` (`"__signature" | "__initials" | "__skip" | "fullName" | "initials" | "email" | "phone" | "date" | "company" | "title" | "address" | \`custom:${string}\``), `SignUserInfo` (fullName required; all other fields optional string; customFields capped at 20 k/v pairs), `SignInput`, `SignFilling`, `SignUnfilled`, `SignResult`, `NoAIProviderConfiguredError`, `SignParseError`, `signPdf()`. Four-stage pipeline: (1) positioned pdfjs extraction via the in-house path (matches ai-redact's approach — returns `pages[{ items[{x,y,w,h,str}], combined, charMap }]` + `ocrCandidatePages` + `fullText`), (2) strict JSON envelope prompt at temperature 0.1 asking for `{ fills: [{ label, anchorText, fieldKey, page, reason }] }` with `postProcessJson` fence stripping + dedicated `SignParseError` class, (3) whitespace-tolerant substring matching per fill — compress `haystack + needle` to lowercased no-whitespace and `indexOf` the anchor, map endpoints back to original item indices, (4) pdf-lib `drawText` placed right-of-anchor baseline with binary-search shrink-to-fit + ellipsize (never overflows the anchor's right-margin). Font rules: special keys `__signature` + `__initials` use `StandardFonts.HelveticaBoldOblique` at 16pt in dark blue `rgb(0.04, 0.07, 0.42)`; all other keys use `StandardFonts.Helvetica` at 10pt black. `__skip` no-ops (model flagged that a label isn't actually a field). `custom:<slug>` resolves against the `customFields` bundle; unknown slugs surface as `unmatched[]`. `normalizeUserInfo()` builds a `valueByKey` Map + `availableKeys` list that's injected into the model prompt so it knows which fields it CAN fill (AI hallucination guard). Returns `{ pdfBytes, signedPdfFilename, markdown, fillings[], unmatched[], pageCount, ocrCandidatePages, providerId, model, usage, wasTruncated }`; markdown summary lists each filled field with page + anchor + reason, unmatched anchors at the bottom, and the explicit non-crypto caveat. (2) `app/api/ai/sign/route.ts` — 429-line, 8-step pipeline mirroring `/api/ai/redact/route.ts` (auth → parseMultipart (pdf + info JSON + idempotencyKey) → validate PDF ≤25 MB + info ≤16 KB (413 `info_too_large`) → `parseUserInfo()` validator (fullName non-empty required; well-known optional fields must be string if present; customFields max 20 {key, value} pairs with empty pairs filtered out) → idempotency replay (kind `"signing"` — returns `signedPdfBase64: null` with flag, client shows "Re-run for a fresh PDF") → `spendCredits(10)` with spend-key `ai:sign:<idempotencyKey>` → `signPdf()` → zero-findings-with->=90%-OCR-pages guard (422 `no_extractable_text` + refund) → `db.transaction` insert files row (filename `"${base} — Fill & Sign Report.md"`) + ai_outputs row with `kind: "signing"` + meta `{ fillings[], unmatched[], signedPdfFilename, pageCount, ocrCandidatePages, providerId, model, tokensIn, tokensOut, wasTruncated, creditCost }`). Route error map: `NoAIProviderConfiguredError` -> 503 no_ai_provider_configured; `SignParseError` -> 502 `sign_parse_failed` + refund; `pdf-extract` failures -> 400 `pdf_extract_failed`; generic -> 502 `sign_failed` + refund. 207 persist-failed fallback still returns the base64 PDF + markdown so nothing the user paid for is lost. TS2352 fix at line 401: `(out as unknown as Record<string, string>)[k] = trimmed;` — needed the `unknown` cast bridge because `SignUserInfo` and `Record<string, string>` don't sufficiently overlap for a direct cast. (3) `components/tools/SignPdfTool.tsx` — `"use client"` runner with the standard auth gate (`useSession()` render-time + `getSession()` defense-in-depth probe, `SIGN_IN_HREF = "/login?callbackUrl=" + encodeURIComponent("/tool/ai-sign")`). State: file, fullName (required, trim-checked for `canSubmit`), initials, email, phone, date (defaults to `todayIso()` = today's YYYY-MM-DD UTC), company, title, address, customFields[{key, value}] array. UI: `ToolDropzone` (PDF, ≤25 MB) + personal-details grid (`repeat(auto-fit, minmax(220px, 1fr))`) + custom-fields table with Add Row / Remove Row controls (capped at 20) + amber-bordered "Not a cryptographic signature" caveat card directing legally-binding needs to DocuSign / Adobe Sign. POST multipart to `/api/ai/sign` with `pdf` + `info` (JSON.stringified bundle) + `idempotencyKey` (crypto.randomUUID). `DownloadSignedButton` decodes `atob(pdfBase64) -> Uint8Array -> Blob("application/pdf") -> anchor click -> URL.revokeObjectURL`. Button labels: "Fill & sign — 10 credits" (authed) / "Sign in to fill & sign" (anon) / "Filling…" (busy). `mapErrorBody()` covers 401/402/409/413 (with `info_too_large` branch)/422/400/502 (with `sign_parse_failed` branch)/503. ResultCard surfaces fill count, per-page provenance, markdown summary, and the non-crypto caveat badge. (4) Wire-up in `app/tool/[id]/page.tsx` — added `"ai-sign"` to `LIVE_TOOL_IDS`, imported `SignPdfTool`, added `case "ai-sign":` branch to the ToolRunner switch, updated the phase-comment header to list all 5 Phase 5.6 tools (AI · Rewrite, AI · Table, AI · Redact, AI · Generate, AI · Sign). No new migration needed — `0003_extend_ai_output_kinds.sql` (from the ai-rewrite ship) already covers the `"signing"` enum value. **Verification:** `npx tsc --noEmit` exit 0 (after the TS2352 unknown-cast fix); pushed `7e0818d..5e85ca0  main -> main`; live prod probes after Hostinger redeploy all green — `GET https://pdfcraftai.com/tool/ai-sign` 200 with "Fill & sign — 10 credits" button + "Drop a PDF to fill & sign" dropzone rendered, `GET https://pdfcraftai.com/api/ai/sign` 405 (POST-only), `POST https://pdfcraftai.com/api/ai/sign` with anon JSON body 401 `{"error":"not_authenticated"}`, `POST https://pdfcraftai.com/api/ai/sign` with anon multipart 401 `{"error":"not_authenticated"}` (auth gate is first in the pipeline, so info validation doesn't leak). Comparison probes confirmed all four Phase 5.6 AI routes (`sign` / `redact` / `generate` / `table`) return 405 on GET — consistent route registration across the whole phase. **Phase 5.6 Coming-Soon set now empty — all five tools (rewrite / table / redact / generate / sign) shipped end-to-end.** (2026-04-20)
- [x] **`/tool/ai-generate` shipped end-to-end (fourth of 5 Phase 5.6 Coming-Soon tools).** Commit `d00ee8a` (2026-04-20). Three new files + one edited wire-up: (1) `lib/ai/generate.ts` — ~620-line helper implementing a two-stage prompt→PDF pipeline. Exports `GenerateDocType` (`"memo" | "report" | "brief" | "letter" | "blog" | "outline" | "other"`), `GenerateLength` (`"short" | "medium" | "long"`), `GenerateTone` (`"neutral" | "formal" | "casual" | "technical"`), `generatePdf()`, `NoAIProviderConfiguredError`. Stage 1: builds a system prompt per doc type + length + tone, asks the model for clean markdown at temperature 0.4 with a `PROMPT_CHAR_BUDGET = 8,000` input clamp and per-length `MAX_TOKENS_BY_LENGTH` output cap (short 900 / medium 2200 / long 4600); extracts stop-reason for `wasTruncated` detection (`"max_tokens"` or `"length"`). Stage 2: custom markdown renderer on top of `pdf-lib` — `parseMarkdown()` regex-splits blocks into `h1` / `h2` / `h3` / `para` / `ulist` / `olist` / `hr`, `parseInline()` tracks bold/italic spans with `hasClosingMarker()` lookahead (unterminated markers fall through as literals). Layout: US-Letter 612×792pt with 72pt (1") margins (468pt content width), `drawHeading()` at 24/18/14pt with gap-before/after, `drawParagraph()` at 11pt with 16pt line height, `drawList()` with bullet indent 14 / text indent 28, `drawHr()` with a 0.5pt rule. Pagination via `ensureSpace(ctx, needed)` which adds a fresh page when `cursorY - needed < marginY`. `ensureLeadingH1()` prepends the title as an H1 if the markdown doesn't already start with `# `. Uses the Helvetica family (Helvetica / Bold / Oblique / BoldOblique) from `StandardFonts` so no font files ship. Word-wrap preserves inline spans across line breaks (including bold-italic combos via independent flag toggles). (2) `app/api/ai/generate/route.ts` — ~290 lines, 8-step pipeline (auth → parseBody (JSON **or** multipart — no PDF upload for this tool) → validate prompt (16,000 char max → 413 `prompt_too_long` with `maxChars`) + docType + length + tone → idempotency replay → spendCredits(20) `ai:generate:<key>` → generatePdf → `db.transaction` insert files + ai_outputs with `kind: "generation"` + meta `{ promptChars, title, docType, length, tone, pageCount, pdfFilename, providerId, model, tokensIn, tokensOut, wasTruncated, creditCost }`). Response carries the generated PDF as **base64** (we don't persist PDF bytes; only the summary markdown lands in `ai_outputs.content_md`, filename `"${title} — Generated.md"`). Refunds on `NoAIProviderConfiguredError` → 503, generic failure → 502 `generate_failed`. Replay branch returns `pdfBase64: null` (bytes aren't persisted; client surfaces "Re-run for a fresh PDF" copy). 207 persist-failed fallback still hands back the base64 PDF + markdown so the user's 20-credit spend isn't lost. `derivePdfFilename(title)` strips `\/:"*?<>|`, caps at 80 chars, appends `.pdf`. (3) `components/tools/GeneratePdfTool.tsx` — ~530 lines. `"use client"` with the same auth gate pattern as ai-rewrite / ai-table / ai-redact (`useSession()` render-time + `getSession()` defense-in-depth probe; `SIGN_IN_HREF = "/login?callbackUrl=" + encodeURIComponent("/tool/ai-generate")`). Form fields: optional title (120-char max), required prompt textarea with a live char counter against 16,000, three selects (Format / Length / Tone). POSTs JSON (not multipart) to `/api/ai/generate`. Button labels: "Generate PDF — 20 credits" (authed) / "Sign in to generate" (anon) / "Generating…" (busy). `DownloadPdfButton` decodes `atob(pdfBase64) → Uint8Array → Blob("application/pdf") → anchor click → URL.revokeObjectURL` to trigger the download. ResultCard renders Generate icon header, provider/model/pageCount/truncation-chip provenance footer, replay-aware copy when `pdfBase64 === null && replay === true`. `mapErrorBody()` covers 401/402/409/413 (with `prompt_too_long` branch returning `maxChars`)/400/502/503. (4) Wire-up in `app/tool/[id]/page.tsx` — added `"ai-generate"` to `LIVE_TOOL_IDS`, imported `GeneratePdfTool`, added `case "ai-generate":` branch, updated the phase-comment to reference Phase 5.6 (AI · Rewrite, AI · Table, AI · Redact, AI · Generate). No new migration needed — `0003_extend_ai_output_kinds.sql` already covers `"generation"`. **Verification:** `npx tsc --noEmit` exit 0; live prod `curl https://pdfcraftai.com/tool/ai-generate` returns 200 with `"Generate PDF — 20 credits"` + `"Describe what you want"` + `"Length"` all rendered; live prod `curl -X POST https://pdfcraftai.com/api/ai/generate -H "Content-Type: application/json" -d '{}'` returns `401 {"error":"not_authenticated"}`; live prod `curl -X POST .../api/ai/generate -H "Content-Type: application/json" -d '{"prompt":"hello"}'` also returns `401 {"error":"not_authenticated"}` (auth gate is first in the pipeline, so prompt validation doesn't leak); live prod `curl https://pdfcraftai.com/api/ai/generate` returns `405` (GET correctly not allowed). Only `ai-sign` remains in the Phase 5.6 Coming-Soon set. (2026-04-20)
- [x] **`/tool/ai-redact` shipped end-to-end (third of 5 Phase 5.6 Coming-Soon tools).** Commit `77278b1` (2026-04-20). Three new files + one edited wire-up: (1) `lib/ai/redact.ts` — ~600-line helper. Because `extractPdfText` strips positions, this file re-does pdfjs extraction in-house via `pdfjs-dist/legacy` + a `createRequire` worker bootstrap, returning `{ pageCount, pages[{ items[{x,y,w,h,str}], combined, charMap }], fullText, ocrCandidatePages }`. The model is asked for a strict JSON envelope `{ findings: [{ category, text, reason }] }` across 12 PII categories (`EMAIL` / `PHONE` / `SSN` / `CREDIT_CARD` / `ADDRESS` / `PERSON_NAME` / `DATE_OF_BIRTH` / `PASSPORT` / `DRIVER_LICENSE` / `IP_ADDRESS` / `API_KEY` / `BANK_ACCOUNT` / `OTHER`) at temperature 0.1; parsing mirrors `lib/ai/table.ts` with a dedicated `RedactParseError` class. For each finding we do whitespace-tolerant substring matching (compress `haystack + needle` to lowercased no-whitespace, run `indexOf`, map endpoints back to original item indices) so a name split by pdfjs into `["John ", "Smith"]` still matches. Every spanned item gets a `pdf-lib` `page.drawRectangle({ color: rgb(0,0,0) })` with ±2px descender padding. Returns `{ pdfBytes, redactedPdfFilename, markdown, findings[], unmatched[], pageCount, ocrCandidatePages, providerId, model, usage, wasTruncated }`; markdown summary includes by-category counts + per-finding table + unmatched table + **explicit security caveat** that this is a visual overlay, the underlying content-stream text is untouched, and print-to-PDF is required for sensitive handoffs. 240k input char budget; 2400 output token cap. (2) `app/api/ai/redact/route.ts` — 8-step pipeline (auth → parseMultipart → validate pdf ≤25 MB → idempotency replay → spendCredits(5) `ai:redact:<key>` → redactPdf → `db.transaction` insert files + ai_outputs with `kind:"redaction"` + `meta.findings[]` + `meta.unmatched[]` + `meta.redactedPdfFilename`). Response carries the redacted PDF as **base64** (we don't persist PDF bytes; only the summary markdown lands in `ai_outputs.content_md`, filename `"${base} — Redaction Summary.md"`). Refunds on `NoAIProviderConfiguredError` → 503, `RedactParseError` → 502 `redact_parse_failed`, extract failures → 400 `pdf_extract_failed`, generic → 502 `redact_failed`. Special case: if the pipeline succeeded with zero findings AND ≥90% of pages were flagged as OCR-candidates, treat as `no_extractable_text` → 422 + refund. Replays return `redactedPdfBase64: null` with a flag — the client shows "Re-run for a fresh PDF" because only the summary is persisted. 207 fallback on persist-failure still hands back the base64 PDF. (3) `components/tools/RedactPdfTool.tsx` — `"use client"` with the same auth gate pattern (`useSession()` render-time + `getSession()` defense-in-depth probe). `DownloadRedactedButton` converts `atob(base64) → Uint8Array → Blob → anchor click` to trigger the download. ResultCard surfaces: redaction count, per-page provenance, explicit amber-bordered security-caveat card ("visual overlay only, print-to-PDF for sensitive handoffs"), and the full markdown summary. `mapErrorBody()` covers 401/402/409/413/422/400/502 (with `redact_parse_failed` copy)/503. Dropzone prompt: "Drop a PDF to redact"; button label: "Redact PDF — 5 credits". (4) Wire-up in `app/tool/[id]/page.tsx` — added `"ai-redact"` to `LIVE_TOOL_IDS`, imported `RedactPdfTool`, added `case "ai-redact":` branch, updated the phase-comment to reference Phase 5.6 (AI · Rewrite, AI · Table, AI · Redact). No new migration needed — `0003_extend_ai_output_kinds.sql` already covers `"redaction"`. **Verification:** `npx tsc --noEmit` exit 0; live prod `curl https://pdfcraftai.com/tool/ai-redact` returns 200 with both `"Drop a PDF to redact"` (dropzone) and `"Redact PDF — 5 credits"` (button) rendered; live prod `curl -X POST https://pdfcraftai.com/api/ai/redact -F "pdf=@/dev/null"` returns `401 {"error":"not_authenticated"}` (auth gate working, route registered); live prod `curl https://pdfcraftai.com/api/ai/redact` returns `405` (GET correctly not allowed). (2026-04-20)
- [x] **`/tool/ai-table` shipped end-to-end (second of 5 Phase 5.6 Coming-Soon tools).** Commit `5205ae1` (2026-04-20). Three new files + one edited wire-up: (1) `lib/ai/table.ts` — provider-agnostic helper that asks the model for a strict JSON envelope `{ tables: [{ title, pageHint, headers[], rows[][] }] }` (temperature 0.1 for determinism), strips optional ```json fences via `postProcessJson`, parses with `JSON.parse` under a dedicated `TableParseError` class (caught explicitly by the route → refund + 502), then fans each table into BOTH a RFC 4180 CSV (`\r\n` line ending, fields quoted on quote/comma/newline, embedded quotes doubled) AND a GFM pipe-table markdown block (pipes escaped to `\|`, newlines normalized to spaces). 240k input char budget matching summarize/rewrite; 3200 output token cap; dedicated `NoAIProviderConfiguredError` mirroring the other AI helpers. Returns `{ markdown, tables: ExtractedTable[], providerId, model, usage, wasTruncated }` so the client can surface per-table CSVs without re-parsing the combined markdown. (2) `app/api/ai/table/route.ts` — 271 lines, 8-step pipeline (auth → parseMultipart → validate pdf + size (≤25 MB) → idempotency replay → spendCredits(3) `ai:table:<key>` → extractPdfText → extractTables → `db.transaction` insert files + ai_outputs with `kind:"table"` + `meta.tables[]`), refund on every error path (pdf_extract_failed / no_extractable_text / table_parse_failed / table_failed / no_ai_provider_configured), 207 fallback on persist-failure that still returns `markdown + tables[]` so nothing the user paid for gets lost, filename derived as `"${base} — Tables.md"`. (3) `components/tools/TableExtractTool.tsx` — 470+ lines. `"use client"` with baked-in auth gate matching the pattern from commit `afcf113`: `useSession()` at render time shows either "Sign in to extract tables" (→ `/login?callbackUrl=/tool/ai-table`) or "Extract tables — 3 credits"; `getSession()` defense-in-depth probe BEFORE the `fetch` routes anons to `/login` without wasting a network round-trip. `mapErrorBody()` covers 401/402/409/413/422/400/502 (with specific `table_parse_failed` copy)/503. `ResultCard` renders a header line (provider + model + page count + truncation chip + credit cost), a markdown preview in a mono pre box, and a per-table card list where each `TableCsvRow` offers Copy CSV (clipboard, RFC 4180 text) + Download .csv (text/csv Blob, filename `"${base} — Table ${idx + 1}.csv"`). Dropzone prompt: "Drop a PDF to extract tables". (4) Wire-up in `app/tool/[id]/page.tsx` — added `"ai-table"` to `LIVE_TOOL_IDS`, imported `TableExtractTool`, added `case "ai-table":` branch to the ToolRunner switch, updated the phase-comment to reference Phase 5.6 (AI · Rewrite, AI · Table). No new migration needed — `0003_extend_ai_output_kinds.sql` (already applied with ai-rewrite) covers the `"table"` enum value. **Verification:** `npx tsc --noEmit` exit 0; live prod `curl https://pdfcraftai.com/tool/ai-table` returns 200 with both `"Drop a PDF to extract tables"` (dropzone label) and `"Extract tables — 3 credits"` (button label) rendered in HTML; live prod `curl -X POST https://pdfcraftai.com/api/ai/table -F "pdf=@small.pdf"` returns `401 {"error":"not_authenticated"}` (auth gate working, route registered). Initial post-redeploy requests 500'd on HTML — stale `next-server` workers from prior deploys didn't know the new route; resolved per CLAUDE.md §5 with SSH `pkill -9 -u u692382124 -f "next-server"` so LiteSpeed's lsnode supervisor spawned fresh workers with the Phase-5.6-aware build. (2026-04-20)
- [x] **`/tool/ai-rewrite` shipped end-to-end (first of 5 Phase 5.6 Coming-Soon tools).** Commit `a5f6f52` (2026-04-20). Eight files: (1) `lib/ai/rewrite.ts` — provider-agnostic helper with 5 modes (`simplify` / `formal` / `casual` / `concise` / `expand`), each with a distinct system prompt; 240k char input budget matching summarize; per-mode output token cap (simplify/formal/casual: 2400, concise: 1600, expand: 4000); temperature 0.3; markdown fence stripping in `postProcessMarkdown`; dedicated `NoAIProviderConfiguredError` class mirroring summarize. (2) `app/api/ai/rewrite/route.ts` — 8-step pipeline (auth → multipart parse → validate mode → idempotency replay → spendCredits(3) → extractPdfText → rewritePdf → `db.transaction` insert files + ai_outputs), 25 MB body cap, refund-on-every-error-path, 207 persist-failed fallback. (3) `components/tools/RewritePdfTool.tsx` — client runner with baked-in auth gate (`useSession()` render-time + `getSession()` defense-in-depth probe before POST, matching the pattern from commit `afcf113`), 5-option radio-card mode picker, result card with Copy / Download / View-on-/app/files buttons, full status-code → user-message error mapping (401/402/409/413/422/400/502/503). (4) `db/migrations/0003_extend_ai_output_kinds.sql` — ALTERs `ai_outputs.kind` enum to include `rewrite` + `table` + `redaction` + `generation` + `signing` in a single migration covering all 5 Phase 5.6 tools so we only touch the DB once. (5) `db/schema/app.ts` — `kind` mysqlEnum extended to match the migration. (6) `lib/pricing.ts` — `AIOperationId` union + `AI_OPERATION_COSTS` extended (rewrite:3, table:3, redact:5, generate:20, sign:10). (7) `lib/ai/idempotency.ts` — `StoredAiOutput.kind` union extended so replay lookup returns the new kinds. (8) `app/tool/[id]/page.tsx` — added `"ai-rewrite"` to `LIVE_TOOL_IDS`, imported `RewritePdfTool`, added `case "ai-rewrite":` branch to the ToolRunner switch. **Verification:** `npx tsc --noEmit` exit 0; migration applied live via SSH `mysql ... -e "ALTER TABLE ai_outputs MODIFY COLUMN kind ENUM(...)"` → `SHOW COLUMNS` confirms 9-value enum `('summary','translation','ocr','comparison','rewrite','table','redaction','generation','signing')`; live prod `curl -X POST https://pdfcraftai.com/api/ai/rewrite -F "mode=simplify"` returns `401 {"error":"not_authenticated"}` (auth gate working); live prod `curl https://pdfcraftai.com/tool/ai-rewrite` returns 200 with mode labels `Simplify / Formal / Casual / Concise / Expand` present in rendered HTML. Deploy required the post-redeploy Hostinger worker kick (CLAUDE.md §5 gotcha) — SSH `pkill -9 -u u692382124 -f "next-server"` cleared stale workers that had exhausted the `uv_thread_create` quota. (2026-04-20)
- [x] **Removed authenticated `/app/studio` Batch + Smart runner; dropped `agent_runs` + `agent_run_steps` tables.** User flagged that the new public `/studio` (workflow canvas) and the old auth'd `/app/studio` (Batch/Smart) were two completely different products both labelled "Studio" — the duality was confusing. Decision: collapse onto the public surface. Per-tool pages (`/tool/ai-summarize`, `/tool/ai-translate`, `/tool/ai-ocr`, `/tool/ai-compare`, `/tool/ai-chat`) remain the real execution surface for signed-in users; the public `/agent` plan-then-review demo + `/macros` library + `/studio` canvas cover the multi-step workflow story without server state. **Deleted (~6,113 lines):** `app/app/studio/page.tsx`, `components/studio/{StudioModeSwitcher,AgentSmartMode,StudioRunner}.tsx` (~2,366 lines combined), `components/studio-public/` (empty dir), `lib/studio/{costs,types}.ts`, `lib/agent-actions.ts`, `lib/agent/{catalog,executor,planner,runner,types}.ts` (~2,778 lines combined). **Schema:** dropped `agentRuns` + `agentRunSteps` from `db/schema/app.ts` (table declarations + their JSDoc) and added `db/migrations/0002_drop_agent_runs.sql` (`DROP TABLE IF EXISTS agent_run_steps; DROP TABLE IF EXISTS agent_runs;`). `userMacros` deliberately preserved because the per-tool `MacroBar` on Summarize + Translate still uses it. **AppShell nav:** removed the "Studio" entry from `components/app/AppShell.tsx` NAV array — signed-in sidebar now reads Dashboard / Files / Chat / API Keys / Billing / Settings. **Stale refs cleaned:** `components/agent/AgentInteractive.tsx` (header comment + Demo Download alert copy), `components/workflow/WorkflowStudio.tsx` (header comment), `lib/workflow/{nodes,agent-plan}.ts` (header comments), `README.md` (Phase 6.2 / 6.3 marked REMOVED, new Phase 6.4 entry), `docs/ai/architecture.md` (Phase 6.2 + Phase 6.3 sections gain REMOVED status banners), `docs/TEST_PLAN.md` (D5 row deleted, D6/D7 renumbered). **Verification:** `rm -rf .next && npx tsc --noEmit` → exit 0; `grep -rn "agentRuns|agentRunSteps|agent_runs|agent_run_steps" app components lib` → only the schema's own removal-banner comment remains; `grep -rn "from.*lib/agent\|from.*lib/studio\|from.*components/studio\b" app components lib` → no matches. The DROP migration must run on prod once via `mysql -h … pdfcraftai_db < db/migrations/0002_drop_agent_runs.sql` to reclaim the two tables; until then the tables exist orphaned, which is harmless. (2026-04-20)
- [x] **`agent_runs` + `agent_run_steps` DROP migration executed on prod MySQL (2026-04-20 follow-up).** SSH into Hostinger (`u692382124@us-imm-web534`), scp'd `db/migrations/0002_drop_agent_runs.sql` to `/tmp`, ran `mysql < /tmp/0002_drop_agent_runs.sql` against `u692382124_pdfcraftai` sourcing the credentials from `~/domains/pdfcraftai.com/public_html/.builds/config/.env`. Pre-drop `SHOW TABLES LIKE 'agent_%'` returned both `agent_run_steps` + `agent_runs`; post-drop returns zero rows. Full post-drop `SHOW TABLES` lists the 16 surviving tables: `accounts`, `ai_outputs`, `api_keys`, `chat_messages`, `chat_sessions`, `credit_ledger`, `credits`, `files`, `password_reset_tokens`, `payments`, `sessions`, `subscriptions`, `user_macros`, `users`, `verification_tokens`, `webhook_events`. App health verified intact: `/api/health` 200 with `{"ok":true,"db":{"ok":true,"latencyMs":2}}`; `/studio`, `/agent`, `/macros` all still 200. Temp SQL file cleaned up from `/tmp` on the server. Closes the orphaned-tables caveat from the prior bullet — the Phase 6.3 Studio cleanup is now fully done both in code and in schema. (2026-04-20)

### Accessibility pass (Lighthouse-driven)

- [x] **Lighthouse mobile audit run on home, /pricing, /tool/page-numbers, /tool/to-pdf, /tool/rotate.** Before scores (a11y): 93 / 94 / 96 / 96 / 96. Commit `aca48fb` shipped three fixes: (a) `--fg-subtle` bumped in `app/globals.css` from `oklch(0.55 0.01 260)` → `oklch(0.68 0.01 260)` in dark mode and `0.58` → `0.45` in light mode — resolved 48 color-contrast failures across all five audited pages (muted metadata, mono captions, pricing footnotes); (b) heading order on home (`components/landing/LandingSections.tsx:592`) promoted from `<h4>` → `<h3>` inside the Audience section after a sibling `<h2>`, and on `/pricing` (`app/pricing/page.tsx:83, 203`) the two top-level `<h3>` section headers (Monthly Plus hero, BYOK) promoted to `<h2>` to sit directly under the page `<h1>`; (c) the `POPULAR` chip on `/pricing` switched from `color:"white"` on `var(--accent)` (2.51 contrast, WCAG fail) to `var(--accent-fg)`. After re-audit: a11y **100** on all five pages. Home perf also moved 79 → 88. Verified live by grepping the production CSS bundle for the new `oklch` value. (2026-04-20)

### Stubs for follow-up email wiring

- [x] **`/api/contact` route** — Zod-validated, in-memory rate-limited, logs submissions until SendGrid/Postmark lands. (2026-04-20)
- [x] **`/api/auth/forgot-password` route** — acks identically on success/miss (anti-enumeration), per-email rate limited, logs for reset-link wiring. `ForgotPasswordForm` now POSTs here instead of the local `setTimeout` mock. (2026-04-20)

### Password reset redemption flow (end-to-end)

- [x] **Password reset-link redemption shipped end-to-end.** Completes the forgot-password → email-link → new-password loop that was previously stubbed.
  - **Schema (`db/schema/auth.ts`)** — new `password_reset_tokens` table: `id` (uuid), `user_id` (FK → `users.id` ON DELETE cascade), `token_hash` (sha256-hex, unique), `expires_at` (ts(3)), `consumed_at` (ts(3), nullable), `created_at` (ts(3), default now). Two indexes: `user_id`, `expires_at`.
  - **Migration (`db/migrations/0001_password_reset_tokens.sql` + `meta/_journal.json`)** — drizzle-kit format, statement-breakpoint-separated, journaled so `pnpm db:push` / `pnpm db:generate` pick it up; also runnable directly via `mysql` CLI on Hostinger as a manual fallback.
  - **Helper (`lib/password-reset.ts`)** — `mintPasswordResetToken(email)` (silent no-op on unknown email, 32-byte random hex raw token, 30-minute TTL, stores only sha256 hash at rest), `lookupPasswordResetToken(raw)` (cheap-rejects non-64-hex before any DB hit), `consumePasswordResetToken(raw, newPassword)` (race-safe single-use via guarded `UPDATE ... WHERE id=? AND consumed_at IS NULL`, bcrypt-10 the new password, then invalidates sibling outstanding tokens for the same user so reset-twice can't leave a live second link).
  - **Forgot endpoint (`app/api/auth/forgot-password/route.ts`)** — rewritten: still anti-enumeration 200 on valid payload, still 1/min per-email rate-limited; now mints the token and logs the full reset URL + email + expiry to the Node process log (Hostinger tail) until a transactional mail provider is wired. `NEXT_PUBLIC_SITE_URL` override respected; falls back to the request origin.
  - **Reset endpoint (`app/api/auth/reset-password/route.ts`)** — new POST. Zod validates 64-hex token + 8–128-char password. Per-IP bucket rate limit (5/min) before the DB. Returns 200 on success, 400 on bad payload, 409 on expired/consumed/missing, 429 on rate limit, 500 on DB error. Same "invalid or expired" message for 409 variants (enumeration-safe).
  - **Dynamic page (`app/reset-password/[token]/page.tsx`)** — async server component, `dynamic = "force-dynamic"`, verifies token server-side on mount. Invalid → renders `AuthShell` with "This reset link won't work" + `<I.Info>` warning card + CTA back to `/forgot-password`. Valid → renders `<ResetPasswordForm token=... />` under "Choose a new password".
  - **Client form (`components/auth/ResetPasswordForm.tsx`)** — twin `PasswordField`s (new + confirm) with show/hide toggles, live `PasswordStrength` indicator, disabled-until (length ≥ 8, matches confirm, strength ≥ 2), inline errors, `router.replace("/login?reset=1")` on 200 (replace, not push — keeps the reset URL out of history).
  - **Middleware (`auth.config.ts`)** — `/reset-password/<token>` added to the "auth page" redirect guard so already-signed-in users bounce to `/app/dashboard` instead of redeeming a token they don't need.
  - **Login flash (`components/auth/LoginForm.tsx`)** — `useSearchParams` reads `?reset=1`; shows a green `<I.Check>` banner ("Password updated. Sign in with your new password to continue.") above the Google SSO button.
  - **Typechecked clean** (`./node_modules/.bin/tsc --noEmit` → `EXIT=0`). Swapping in Resend/Postmark once the mailbox is live is a single drop-in around the `console.log` in the forgot endpoint. (2026-04-20)

### Search engines

- [x] **GSC: sitemap resubmitted.** Property `https://pdfcraftai.com/`. Discovered pages refreshed 59 → 39. (2026-04-20)
- [x] **Bing Webmaster Tools: sitemap resubmitted.** Status moved Success → Processing, last-submit 2026-04-19. URL count will refresh from stale 21 → 39 after re-crawl. (2026-04-20)

### Security / housekeeping

- [x] **Old over-scoped GitHub PAT (`cowork-pdfcraftai-deploy`) deleted.** Had `admin:enterprise, admin:org, delete_repo` etc., expired 2026-05-19. (2026-04-20)
- [x] **Active PAT is `cowork-pdfcraftai-deploy-v2`** — minimal scopes (`repo, workflow, read:network_configurations`), expires 2026-07-18. Stored in `.claude/secrets.env`. (2026-04-20)
- [x] **Hostinger SSH key `cowork-apr2026-v2` active.** Private half at `.claude/id_ed25519_cowork`. (2026-04-19)
- [x] **`.gitignore` covers `.claude/`, `secrets.env`, `id_ed25519*`, `*.key`, `*.pem`, `*.pub`.** Synced from local mount into the repo. (2026-04-20)
- [x] **`CLAUDE.md` + `docs/DEPLOYMENT_NOTES.md` + `docs/STATUS.md` versioned in the repo.** Survives sandbox wipes and fresh clones. (2026-04-20)
- [x] **(SEV-2 CLOSED) Per-page `openGraph` + `twitter` blocks now override the root-layout defaults.** Every non-home public route had been shipping the homepage's generic `og:title` + `og:description` on Twitter/LinkedIn/Slack share cards because Next 14 App Router shallow-merges metadata and child pages were only setting top-level `title` / `description`. Two-part fix: (a) root `app/layout.tsx` now declares `openGraph.title` and `twitter.title` as `{ default, template: "%s · pdfcraft ai" }` objects so every child page's `title` automatically becomes the social-card title; (b) new reusable `lib/page-metadata.ts` helper (`pageMetadata()` + `PageMetadataInput` type) collapses `title` / `description` / `openGraph` / `twitter` / `alternates.canonical` into a single `export const metadata = pageMetadata({ … })` call — adopted by `/about`, `/contact`, `/gdpr`, `/careers`, `/status`, `/changelog`, `/bulk`, `/macros`. Remaining public pages (`/help`, `/tools`, `/privacy`, `/security`, `/terms`, `/dpa`, `/blog` + `/blog/[slug]`, `/translate-pdf`, `/split-pdf`, `/merge-pdf`, `/pdf-to-word`, `/compress-pdf`, `/agent`, `/studio`, `/tool/[id]`) got matching `twitter` blocks added alongside their existing `openGraph` blocks; `/help/[slug]`'s pre-existing `twitter.card: "summary"` was bumped to `"summary_large_image"` for site-wide consistency. Auth pages (`/login`, `/register`, `/signup`, `/forgot-password`, `/account`) are `robots: { index: false, follow: false }` and intentionally inherit the root template (`Sign in · pdfcraft ai` etc) — no per-page override needed, that's the correct design. 28 files changed; `npx tsc --noEmit` exit 0. Commit `d035635`. (2026-04-20)
- [x] **(SEV-3 CLOSED) Root canonical + `/api` twitter:card hygiene.** Added `export const metadata: Metadata = { alternates: { canonical: "/" } }` to `app/page.tsx` — the root route was the only page in the app shipping no `<link rel="canonical">`. Also standardised `app/api/page.tsx` twitter:card from `summary` to `summary_large_image` so the API reference shares the same-size Twitter/X share card as every other route. `npx tsc --noEmit` exit 0. (2026-04-20)
- [x] **(P0 auth end-to-end smoke — zero defects.)** Drove the full NextAuth credentials flow from `curl` against prod with test account `rajasekarjavaee+5@gmail.com` (id `4e20c284-…`), topped up to 1,000 credits via direct `UPDATE credits SET balance = balance + 1000 … ; INSERT INTO credit_ledger …` with idempotency key `cowork-test-topup-20260420`. **A1** login: `GET /api/auth/csrf` → `POST /api/auth/callback/credentials` returned `HTTP/2 302` + `location: /app/dashboard` + `__Secure-authjs.session-token` JWE cookie (HttpOnly, Secure, SameSite=Lax, 30-day expiry). **A1b** wrong password and **A1c** non-existent user both returned **identical** `location: /login?error=CredentialsSignin&code=credentials` — anti-enumeration response shape confirmed, no timing/content signal distinguishing the two cases. **A2** duplicate-email guard: direct `INSERT` with the existing email rejected by `ERROR 1062 'users_email_unique'` — DB UNIQUE constraint backs up the app-level pre-check in `registerAction`. **A3** authed access: `GET /app/dashboard` with session cookie returned `HTTP 200`; after signout the same request returned `HTTP/2 307` + `location: /login?callbackUrl=/app/dashboard`. **A4** signout: `POST /api/auth/signout` returned `HTTP 302 → /`; `GET /api/auth/session` then returned `null`. **A5** session endpoint returned `{"user":{"name":"Rajasekar Selvam","email":"rajasekarjavaee+5@gmail.com","id":"4e20c284-…"},"expires":"2026-05-20T…"}`. **A7b** middleware-while-authed: `/login`, `/register`, `/forgot-password` all returned `HTTP 302 → /app/dashboard`; `/signup` returned `HTTP 307 → /register` (platform redirect runs first; middleware catches the subsequent `/register` request and 302s to `/app/dashboard` on the hop — `curl -L` resolves to dashboard correctly). **Not covered in this sweep** (deferred): Google OAuth interactive consent (needs user to click through on `arjundass7106@gmail.com`); `registerAction` via RSC server-action wire format (too opaque to drive from curl, but the test account was created live today via that same action so the flow is covered circumstantially). (2026-04-20)
- [x] **(Auth hygiene) `/signup` alias moved from a server component to a platform-level redirect in `next.config.mjs`.** Previously `app/signup/page.tsx` called `redirect("/register")` from `next/navigation` at module level, which Next's static prerender cached as `HTTP/2 307` with **no `Location` header** and an `<html id="__next_error__">` body — so in-app RSC client-side nav worked, but direct HTTP hits (`curl -L`, search-engine crawlers, external links, bookmarks) hit a dead 307 they couldn't follow. Fix: added `async redirects()` to `next.config.mjs` returning `[{ source: "/signup", destination: "/register", permanent: false }]` and deleted `app/signup/page.tsx`. Middleware in `auth.config.ts` still redirects authed users off `/signup` before `redirects()` runs, so the authed-user flow is preserved. `next build` confirmed `/signup` is absent from the route manifest; `/register` still builds; zero new warnings. Verified live post-deploy: `curl -sI https://pdfcraftai.com/signup` now returns `HTTP/2 307` + `location: /register` + `cf-cache-status: DYNAMIC` (stale `x-nextjs-cache: HIT` gone); `curl -sL` follows cleanly to `https://pdfcraftai.com/register` with `HTTP 200`. Post-deploy smoke across `/`, `/login`, `/register`, `/forgot-password`, `/pricing`, `/help`, `/api/health`, `/sitemap.xml`, `/robots.txt` all green (HTTP 200), no 503 from the redeploy. Commit `e39005e`. (2026-04-20)
- [x] **(Title hygiene) Stripped redundant "— pdfcraft ai" suffix from 16 page-level `title` / `openGraph.title` / `twitter.title` strings.** After the SEV-2 fix landed `app/layout.tsx` with `title.template: "%s · pdfcraft ai"`, any page still ending its title with "— pdfcraft ai" started rendering doubled (e.g. `Pricing — pdfcraft ai · pdfcraft ai`). Fix strips the suffix from `/api`, `/tools`, `/blog` + `/blog/[slug]`, `/careers`, `/changelog`, `/dpa`, `/gdpr`, `/help` + `/help/[slug]` (help articles now render `<title> — help · pdfcraft ai`), `/pricing`, `/privacy`, `/security`, `/status`, `/terms`, `/tool/[id]` — 16 files, 38 lines each direction. `npx tsc --noEmit` exit 0. Commit `8d745ff`. Verified live on prod: `curl https://pdfcraftai.com/{api,tools,blog,changelog,help,security,terms,privacy,dpa,careers,status,gdpr,pricing,tool/merge,tool/split,tool/compress}` all now render `<Title> · pdfcraft ai`; `/help/api-quickstart` renders `Quickstart in 5 minutes — help · pdfcraft ai`. (2026-04-20)
- [x] **(SEV-1 CLOSED) Full CSP now lands on every response — enforced at the Apache/LiteSpeed layer via `.htaccess`.** Root cause: the LiteSpeed/Passenger path was stripping the Node-set `Content-Security-Policy` header down to just `upgrade-insecure-requests` in prod, even though `next.config.mjs` composed a full multi-directive policy. Fix: added a `<IfModule mod_headers.c>` block to `/home/u692382124/domains/pdfcraftai.com/public_html/.htaccess` with `Header always unset Content-Security-Policy` followed by `Header always set Content-Security-Policy "<full policy>"`. Backup of the pre-CSP `.htaccess` at `.htaccess.bak-pre-csp-20260420T084118Z` on the origin. Also widened `next.config.mjs` ANALYTICS_ORIGINS into three categorized arrays (`_SCRIPT` for GTM + Clarity script loaders, `_CONNECT` for GA4 + Clarity beacons + googletagmanager, `_IMG` for GA4 + GTM pixels) so the origin-configured CSP matches the edge-enforced one byte-for-byte — belt-and-braces. Verified live: `curl -sI "https://pdfcraftai.com/?_csp_probe=…"` returns the full `content-security-policy:` header with all directives (`default-src 'self'` … `frame-src 'self' <razorpay+paypal>` … `connect-src 'self' <razorpay+paypal+analytics>` … `frame-ancestors 'none'; upgrade-insecure-requests`). PCI SAQ-A scope lockdown that the `next.config.mjs` comments promise is now real in production. (2026-04-20)

---

## Pending (Claude can do — pick these up first)

### Pre-audit carryovers

- [x] **(quality) Home-page TBT pass — pre-hydrate NextAuth session on the server.** Commit `366ad75`. Previously, `components/providers/SessionProviderWrapper.tsx` mounted `<SessionProvider>` with no `session` prop, so every page hit (including logged-out home-page visits) fired a `/api/auth/session` network request on mount and re-rendered the entire TopNav client tree once the response came back. That round-trip was ~150–300 ms of TBT on the critical path. Fix: (a) `app/layout.tsx` is now `async` and pre-resolves the session via the NextAuth v5 `auth()` helper — for logged-out visitors this is ~instant (no DB hit, just a missing-cookie short-circuit); for logged-in visitors it returns the same session the client would have fetched. (b) `SessionProviderWrapper` now takes a `session: Session | null` prop and forwards it to `<SessionProvider session={session} refetchOnWindowFocus={false}>`, so the client tree hydrates with state already populated AND doesn't re-fetch on every window focus. Prior perf notes in this entry — "hero image not marked priority" — were stale: the homepage has no hero image (confirmed by reading `app/page.tsx` + `HeroDemo.tsx`, both pure text + CSS/SVG); "GA4/Clarity not on lazyOnload" was already done in commit `36034eb`. LandingSections + ToolsShowcase are pure server components (no `"use client"`), so below-fold dynamic-import carving would not reduce the client bundle. Home-page First Load JS unchanged at 100 kB shared; the win is at runtime. `tsc --noEmit` exit 0. `next build` exit 0 (the `MissingSecret` build-time warning is harmless — `NEXTAUTH_SECRET` is only missing in the sandbox; Hostinger prod has it wired). (2026-04-20)
- [x] **(quality) `/api/health` now reports deployed commit SHA — no hPanel env vars needed.** Commit `5a59614`. Baked in at build time via `next.config.mjs` — a pre-config `execSync("git rev-parse --short=12 HEAD")` block captures the short SHA and the new `env: { BUILD_COMMIT_SHA }` block inlines it into the bundle; `app/api/health/route.ts` reads `process.env.BUILD_COMMIT_SHA` (with fallbacks to `COMMIT_SHA` / `NEXT_PUBLIC_COMMIT_SHA` / `VERCEL_GIT_COMMIT_SHA`). Falls through to `null` if `git` isn't available at build time. Verified locally: `grep c632fab .next/server/app/api/health/route.js` → match; `next build` exit 0. Post-deploy had the standard Hostinger stale-worker lag (10 workers round-robining between old `commit:null` and new `commit:"5a59614e2dd9"` for ~8 minutes); resolved per CLAUDE.md §5 via SSH `pkill -9 -u u692382124 -f next-server` — fresh worker respawned and all subsequent probes (5/5) returned `{"ok":true,"commit":"5a59614e2dd9","db":{"ok":true,"latencyMs":2..32}}`. Post-kick regression: 8/8 routes (`/`, `/login`, `/register`, `/pricing`, `/tool/merge`, `/tool/ai-sign`, `/sitemap.xml`, `/robots.txt`) return 200. Unblocks the Cloudflare origin-health-check line item (we can now ask CF to watch for `commit:` presence in the body, not just `ok:true`). (2026-04-20)
- [ ] **(monitoring) Wire Cloudflare origin health check at `/api/health`.** Endpoint is live (see Done above); this is just the last-mile step of pointing CF at it in the dashboard.

---

## Zero-issue production readiness sweep (2026-04-20, post-TBT-fix)

Comprehensive live audit after commit `0a5932c9cadf` (the TBT pre-hydration fix) went live. Method: direct `curl` probes against the Cloudflare-proxied apex. **Result: one open SEV-1 (AI provider key in hPanel, blocked on user), everything else clean.**

| Area | Probe | Result |
|---|---|---|
| Security headers | `curl -sI /` | Full CSP, HSTS `max-age=63072000`, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy camera/mic/geo denied + payment allowlist — **all present** |
| Canonical tags | 7 public pages | All public pages carry `<link rel="canonical">` to their apex URL. `/login` correctly omits canonical (page is `robots: noindex, nofollow`) |
| API contracts | 10 `/api/ai/*` endpoints | All 10 return `POST=401 not_authenticated`, `GET=405` — contracts match |
| Auth endpoints | 3 NextAuth routes | `/api/auth/session` 200 (null body for anon), `/api/auth/providers` 200, `/api/auth/csrf` 200 |
| Sitemap | `/sitemap.xml` | 63 URLs listed, application/xml |
| Robots | `/robots.txt` | Allow: /, Disallow: /api/ /app/ /admin/ /_next/, Sitemap + Host advertised, Cloudflare managed content signals block AI training crawlers (GPTBot, ClaudeBot, Google-Extended, Applebot-Extended, Bytespider, CCBot, etc.) |
| 404 behavior | `/this-does-not-exist-xyz` | Returns 404 (not a 200 soft-404) |
| Legal pages | 7 paths | /privacy /terms /dpa /gdpr /security /about /contact — **all 200** |
| SEO landing pages | 5 paths | /merge-pdf /split-pdf /compress-pdf /pdf-to-word /translate-pdf — **all 200** |
| Free tool runners | 8 IDs in `lib/tools.ts` | /tool/merge /tool/split /tool/compress /tool/rotate /tool/page-numbers /tool/to-pdf /tool/protect /tool/pdf-to-office — **all 200**. Non-existent IDs (/tool/reorder, /tool/watermark, /tool/unlock) correctly 404 — those features are merged into parent tools (rotate+reorder, page-numbers+watermark, protect+unlock) |
| AI tool runners | 10 IDs | /tool/ai-summarize /tool/ai-translate /tool/ai-ocr /tool/ai-chat /tool/ai-compare /tool/ai-rewrite /tool/ai-table /tool/ai-redact /tool/ai-generate /tool/ai-sign — **all 200** |
| Marketing surfaces | 10 paths | /tools /agent /macros /studio /bulk /changelog /careers /status /api /blog — **all 200** |
| Redirects | /signup → /register | Platform redirect (`redirects()` in `next.config.mjs`) — 307 with `Location: /register` header intact |
| Auth gate | /app/* paths | 5 routes (/app/dashboard /app/files /app/billing /app/settings /app/account) all return 307 → `/login?callbackUrl=...` with callback properly URL-encoded. /app/nonexistent also redirects (middleware catches before 404 render) |
| Health probe | 5× `/api/health` | All 5 return `ok: true, commit: 0a5932c9cadf, db.ok: true, latencyMs: 1–36ms` (consistent SHA = single-worker state, no stale workers round-robining) |

**Single remaining open SEV-1:** `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` missing from Hostinger runtime env — all 10 AI routes currently return 503 `ai_provider_not_configured` when reached by an authenticated caller. Blocked on user (hPanel → Websites → pdfcraftai.com → Advanced → Node.js → Environment Variables → Save & Redeploy). Tracked below in Pending (needs the user).

---

## Pending (needs the user — Claude cannot complete autonomously)

### Email authentication (only after the user picks an email host)

- [ ] **DMARC TXT record at `_dmarc.pdfcraftai.com`** in Cloudflare DNS. Suggested start: `v=DMARC1; p=none; rua=mailto:dmarc-reports@pdfcraftai.com; pct=100; aspf=r; adkim=r`. Move to `p=quarantine` then `p=reject` after a few weeks of clean reports.
- [ ] **Custom DKIM key in Hostinger Email** (or whatever email provider gets chosen). Hostinger generates the key; user pastes the resulting CNAME/TXT into Cloudflare DNS.
- [ ] **SPF TXT record at apex** (`v=spf1 include:_spf.hostinger.com -all` or include the chosen sender's SPF host).
- [ ] **`support@pdfcraftai.com` mailbox.** Confirm it sends + receives end-to-end before swapping it into the Google OAuth contact email.
- [ ] **Transactional sender wired** (Resend / Postmark / Hostinger SMTP) for password resets, magic links, receipts, etc. Needs an account + API key the user creates.

### Manual smoke tests

- [ ] **Google sign-in click-test.** Open `/login` in your browser, click "Continue with Google", complete the round-trip, confirm redirect back to the app and a session cookie is set. Claude can drive `/login` via Chrome MCP but cannot complete the Google account login itself.

### Cloudflare audit (10-item review, when convenient)

- [ ] SSL/TLS mode = Full (strict) confirmed
- [ ] HSTS on (already present in response headers)
- [ ] Always Use HTTPS on
- [ ] Auto Minify on for HTML/CSS/JS
- [ ] Brotli on
- [ ] Bot Fight Mode review (avoid breaking legit API calls)
- [ ] WAF rules: rate-limit `/api/auth/*` to ~10 req/min/IP
- [ ] Page Rules: confirm www → apex 301
- [ ] Email routing for `support@`, `dmarc-reports@` if mail isn't on Hostinger
- [ ] Analytics → confirm Web Analytics is on (free, separate from GA4)

---

## Credential reference

All actual credential values live ONLY in `.claude/secrets.env` (gitignored). If that file is missing in a fresh sandbox, see `CLAUDE.md` §4 for the handoff pattern — Claude will ask you to paste them.

| Credential | Stored in | Notes |
|---|---|---|
| GitHub PAT (`cowork-pdfcraftai-deploy-v2`) | `.claude/secrets.env` as `GITHUB_PAT` | Expires 2026-07-18 |
| Hostinger SSH private key | `.claude/id_ed25519_cowork` | Public half registered as `cowork-apr2026-v2` |
| Hostinger env vars | hPanel only | Never copied to sandbox |
| Google OAuth client secret | Hostinger env vars only | Never copied to sandbox |
